😂 Fair point. I shouldn’t invent claim numbers. We’ve been using real data throughout this project.

Let’s use your actual claim:

Claim Number = CLM-2026-1859

Everything should be validated against that.

⸻

Step 1 - Verify Claim Exists

SELECT
    id,
    claim_number,
    status,
    location,
    policyholder_name
FROM claims
WHERE claim_number = 'CLM-2026-1859';

Expected

* Exactly 1 row
* Note down:
    * id
    * status
    * location

These will later be copied into work_orders.

⸻

Step 2 - Find an Active Expert

Instead of guessing EXP-1001, let’s query the database.

SELECT
    expert_id,
    name,
    expert_type,
    active
FROM experts
WHERE active = true
LIMIT 5;

Pick one expert from the results.

Suppose it returns

EXP-0007
John Smith
Fire Damage Expert
true

We’ll use that exact expert_id.

⸻

Step 3 - Ensure No Active Work Order Already Exists

SELECT *
FROM work_orders
WHERE claim_number='CLM-2026-1859'
AND status IN ('Scheduled','In Progress');

Expected

0 rows

If rows exist, we’ll either complete/cancel that work order or use another claim.

⸻

Step 4 - Ensure Expert Isn’t Already Booked

Replace EXP-0007 with the expert you selected.

SELECT *
FROM work_orders
WHERE expert_id='EXP-0007'
AND scheduled_date='2026-08-06'
AND scheduled_time='14:30:00'
AND status IN ('Scheduled','In Progress');

Expected

0 rows

⸻

Step 5 - API Call

Now we finally call Dispatch.

curl -X POST "http://localhost:8900/api/dispatch/work_orders" \
-H "Content-Type: application/json" \
-d '{
  "claim_id":"IGNORE",
  "claim_number":"CLM-2026-1859",
  "expert_id":"EXP-0007",
  "expert_name":"IGNORE",
  "expert_type":"IGNORE",
  "scheduled_date":"2026-08-06",
  "scheduled_time":"2:30 PM",
  "customer_address":"IGNORE",
  "assigned_by":"Adjuster-1",
  "estimated_arrival":"30 mins",
  "estimated_cost":2500,
  "priority":"Normal",
  "notes_to_expert":"Inspect fire damage",
  "customer_phone":"9999999999",
  "customer_email":"abc@test.com"
}'

⸻

After that we’ll verify exactly what got inserted into:

* ✅ work_orders
* ✅ dispatch_logs

We’ll compare every column with the source tables (claims and experts) to make sure the handler is pulling the correct values instead of trusting the request.

Let’s do it with your actual database. First, run the first two SQL queries (claim lookup and active experts) and paste the results. Then I’ll generate the exact curl command using your real expert_id instead of a made-up one.
