Found it — there's already a real "Complete Investigation" button wired to POST /api/adjuster/complete-investigation. Let me find the server-side handler for that route.
Ran 5 commands
Confirmed — same host (claimsagenticdb.postgres.database.azure.com), same shared database as every Python agent. My earlier "zero writers" claim was wrong: I only grepped --include=*.py, and missed this TypeScript app entirely.
You're right — that button is the real, working writer
claims-solution-integration (a Vite/Node app, separate from the Python agent servers) has a fully functional "Complete Investigation" button:
UI: src/pages/adjuster/case-investigation.tsx:1116 — the button calls completeInvestigation(), which does POST /api/adjuster/complete-investigation?claimNumber=..., disabled unless intakeValidation.status === "Complete" and not already completed.
Server route: vite-plugins/adjuster.ts:1909 — routes /complete-investigation to handleCompleteInvestigation().
DB write: vite-plugins/adjuster.ts:1733:
— idempotent (checks for an existing row first and returns alreadyCompleted: true rather than duplicating).
Plain Text
INSERT INTO claim_investigation_completion (claim_id, completed_by)VALUES ($1, $2)RETURNING completed_by, completed_at


Connection: AZURE_DATABASE_URL in this app's own .env, pointing to claimsagenticdb.postgres.database.azure.com — verified it's the same Postgres instance/database every Python agent uses.
So there's no implementation gap here at all — I was wrong to say "nothing populates it." It's a deliberate manual sign-off (matches the SQL comment "plain manual sign-off, not a check that can fail and need justifying"), gated behind claim_intake_validation being complete, done by a human adjuster clicking a button in the portal rather than by an AI agent. That's architecturally sensible: whether the investigation is actually done is a human judgment call, not something SIU's Closure checklist should infer automatically.
Practical implication for testing check_closure_readiness: to make investigator_report_submitted become true for CLM-2026-1001 the "real" way, you'd run the claims-solution-integration dev server and click that button on the claim's Case Investigation page (after Intake Validation is marked complete) — rather than me hand-inserting a row. If you'd rather not stand up that UI right now, I can still insert a row directly for a quick test, but the button is the correct path going forward.
Want me to do the same cross-check for vendor_network_signals — search claims-solution-integration (and hexaware-claims-portal) for a UI-driven writer before we assume that one's a genuine gap too?
lets do it this way once adjuster clicks on complete investigation button then claim_investigation_completion table should be popultaed accordingly
Showed full handleCompleteInvestigation function implementation
The logic in handleCompleteInvestigation is already exactly this — it's gated on claim_intake_validation, idempotent, and inserts into claim_investigation_completion correctly. No code changes needed there. Let me verify end-to-end for our test claim by checking the gate first, then walking the same path the button triggers.
Checked whether the gate is satisfied and whether a row already exists for the test claim
Good call — this confirms the design is already correct and requires no code change. But testing it for our claim (CLM-2026-1001) surfaced something: it currently has no row in claim_intake_validation, and the button's server-side gate explicitly requires validation_passed or overridden to be true there before it will insert into claim_investigation_completion. So right now, clicking "Complete Investigation" for this claim would correctly be blocked (both the UI button is disabled, and the server would return a 400) — that's the gate working as designed, not a bug.
To actually prove the full chain end-to-end (button → claim_investigation_completion → SIUClosureAgent sees investigator_report_submitted: true), I'd need to first satisfy that intake-validation gate for this claim — which touches a different table (claim_intake_validation) outside what we've been fixing.
How would you like to verify the full chain end-to-end for CLM-2026-1001?
Simulate it directly (Recommended)
Checked claim_intake_validation schema to build a valid gating row
Now let's insert a satisfying claim_intake_validation row, then run the exact same guarded logic the button's server route runs, then confirm SIUClosureAgent picks it up.
Satisfy the intake validation gate for the test claim
Now replicating exactly the same guarded logic handleCompleteInvestigation runs (gate check → idempotency check → insert):
Simulate the button click's exact server-side logic for CLM-2026-1001
Now let's confirm SIUClosureAgent picks this up correctly.
Re-ran SIUClosureAgent's closure readiness check to confirm it now sees investigator_report_submitted:true
Confirmed end-to-end. The chain works exactly as intended:
Gate: claim_intake_validation.validation_passed = true for CLM-2026-1001 (simulating a passed intake check)
Button click → POST /api/adjuster/complete-investigation → handleCompleteInvestigation() → INSERT INTO claim_investigation_completion (claim_id, completed_by) VALUES ('CLM-2026-1001', 'adjuster_1')
SIUClosureAgent.check_closure_readiness('SIU-2026-0001') now reports investigator_report_submitted: true — flipped from false in every prior run this session.
All four other checklist items are now also true (evidence_correlation_complete, network_analysis_complete, behavioral_analysis_complete — from the tables we populated/fixed earlier). Only final_decision_recorded remains false, which correctly requires a separate action: FraudResolutionAgent.write_siu_decision/resolve_siu_case writing to siu_decision.
No code changes were needed anywhere in this step — claims-solution-integration's existing button/route/handler was already correct; the earlier "nothing populates it" claim was my mistake from only having grepped Python code. The finding to remember: manual-signoff tables in this platform (claim_investigation_completion, and likely claim_intake_validation) are populated by the claims-solution-integration UI, not by any Python SIU/Adjuster agent — worth checking that UI first before assuming a gap, for any similar question going forward.
