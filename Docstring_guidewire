testsplitters

# from langchain.document_loaders import PyPDFLoader, TextLoader
from langchain_community.document_loaders import TextLoader
loader = TextLoader("C:\\Users\\2000137378\\Desktop\\newproject\\RAG_PIPELINES\\mytext.txt",encoding ="utf-8")
text = loader.load()

print(text)


# TextLoader
# PyPDFLoader
# CSVLoader
# JSONLoader
# DocxLoader

# FLOW
# file.txt
#   ↓
# read file
#   ↓
# convert text
#   ↓
# Document object

# OUTPUT
# documents is NOT text.
# It is a list (array) of Document objects.documents is NOT text.
# It is a list (array) of Document objects.

# [
# Document(
# page_content="text inside file",
# metadata={'source':'file.txt'}
# )
# ]



# TEXT SPLITTING


from langchain_text_splitters import (RecursiveCharacterTextSplitter, CharacterTextSplitter, TokenTextSplitter)
from langchain_community.document_loaders import TextLoader


loader = TextLoader("C:\\Users\\2000137378\\Desktop\\newproject\\RAG_PIPELINES\\mytext.txt",encoding ="utf-8")
document = loader.load()

# print(document)

###Character text splitter
text = document[0].page_content
text_splitter = CharacterTextSplitter(separator="\n", chunk_size=100, chunk_overlap=20,length_function = len)
#chunk size is characters 

char_chunk = text_splitter.split_text(text)

print(len(char_chunk))
# print(f"first Chunk: {char_chunk[0][:100]}...")


### Recursive character text splitter
recursive_char_splitter = RecursiveCharacterTextSplitter(chunk_size=100, chunk_overlap=20)
recursive_char_chunk = recursive_char_splitter.split_text(text)
print(len(recursive_char_chunk))
# print(f"first Chunk: {recursive_char_chunk[0][:100]}...")

# 1️⃣ Example text (like your example)
# Let’s create text like this:
# text = """Short para.
# This is a very long paragraph that contains many many words and keeps going on and on to simulate a big paragraph which should exceed the chunk size limit so that we can observe how recursive splitting works internally.
# Another short para."""
# Assume:
# chunk_size = 50
# chunk_overlap = 0

# ⸻
# 2️⃣ Code we run
# from langchain_text_splitters import RecursiveCharacterTextSplitter
# text = """Short para.
# This is a very long paragraph that contains many many words and keeps going on and on to simulate a big paragraph which should exceed the chunk size limit so that we can observe how recursive splitting works internally.
# Another short para."""

# splitter = RecursiveCharacterTextSplitter(
#    chunk_size=50,
#    chunk_overlap=0
# )

# chunks = splitter.split_text(text)

# for i, chunk in enumerate(chunks):
#    print("Chunk", i+1)
#    print(chunk)
#    print("Length:", len(chunk))
#    print()

# ⸻
# 3️⃣ Now let’s simulate what happens internally
# Default separators:
# ["\n\n", "\n", " ", ""]
# Order matters.
# ⸻
# Step 1 — Try paragraph split (\n\n)
# Text splits into:
# Piece 1:
# Short para.
# Length:
# 11 chars ✔
# Good → becomes chunk.
# ⸻
# Piece 2:
# This is a very long paragraph that contains many many words and keeps going on and on to simulate a big paragraph which should exceed the chunk size limit so that we can observe how recursive splitting works internally.
# Length:
# ~220 chars ❌
# Too big.
# So recursive splitting starts.
# ⸻
# Piece 3:
# Another short para.
# Length:
# 20 chars ✔
# Good → becomes chunk.
# ⸻
# 4️⃣ Now recursive splitting starts on Piece 2
# Try next separator:
# \n
# But no newline inside.
# Still ~220 ❌
# Try next separator:
# space
# Now split into words:
# This
# is
# a
# very
# long
# paragraph
# that
# contains
# many
# many
# words
# ...

# ⸻
# 5️⃣ Now it starts building chunks under 50 chars
# It starts combining words:
# Build chunk:
# This is a very long paragraph that contains many
# Check length:
# ~48 ✔
# Chunk created.
# ⸻
# Next words:
# many words and keeps going on and on to simulate
# Length:
# ~47 ✔
# Chunk created.
# ⸻
# Next:
# a big paragraph which should exceed the chunk
# ~46 ✔
# Chunk created.
# ⸻
# Next:
# size limit so that we can observe how recursive
# ~48 ✔
# Chunk created.
# ⸻
# Next:
# splitting works internally.
# ~27 ✔
# Chunk created.
# ⸻
# 6️⃣ Final result becomes:
# Chunks:
# Chunk 1:
# Short para.
# Chunk 2:
# This is a very long paragraph that contains many
# Chunk 3:
# many words and keeps going on and on to simulate
# Chunk 4:
# a big paragraph which should exceed the chunk
# Chunk 5:
# size limit so that we can observe how recursive
# Chunk 6:
# splitting works internally.
# Chunk 7:
# Another short para.

# ⸻
# 7️⃣ What we learned from this example
# Recursive splitter did:
# Step 1:
# Split by paragraph.
# Short ✔
# Long ❌
# Short ✔
# Step 2:
# Long paragraph too big → try next separator.
# Step 3:
# Split by words.
# Step 4:
# Combine words until chunk_size reached.
# ⸻
# 8️⃣ The key insight (this is the main point)
# Recursive splitter tries:
# Keep structure if possible
# But if structure violates chunk_size:
# Break structure gradually
# Meaning priority:
# Structure first
# Size enforcement second

# ⸻
# 9️⃣ Now let’s add overlap example
# If:
# chunk_overlap = 10
# Then:
# Chunk2:
# This is a very long paragraph that contains many
# Chunk3 would start with last 10 chars:
# Example:
# tains many words and keeps going on and on
# Overlap part:
# tains many
# Repeated.
# ⸻
# 🔟 The simplest mental model (best way to remember)
# Recursive splitter behaves like:
# “I will try to cut nicely.
# But if text is too big,
# I will cut more aggressively.”
# ⸻
# 11️⃣ If you want the next thing that makes this crystal clear:
# Next we should do:
# Same text with CharacterTextSplitter vs RecursiveCharacterTextSplitter side-by-side
# That comparison usually removes 100% confusion.
# Just say:
# “Compare both on same text”Perfect. Let’s take a real working example and trace exactly what RecursiveCharacterTextSplitter does internally. We will literally simulate its thinking.
# ⸻
# 1️⃣ Example text (like your example)
# Let’s create text like this:
# text = """Short para.
# This is a very long paragraph that contains many many words and keeps going on and on to simulate a big paragraph which should exceed the chunk size limit so that we can observe how recursive splitting works internally.
# Another short para."""
# Assume:
# chunk_size = 50
# chunk_overlap = 0

# ⸻
# 2️⃣ Code we run
# from langchain_text_splitters import RecursiveCharacterTextSplitter
# text = """Short para.
# This is a very long paragraph that contains many many words and keeps going on and on to simulate a big paragraph which should exceed the chunk size limit so that we can observe how recursive splitting works internally.
# Another short para."""

# splitter = RecursiveCharacterTextSplitter(
#    chunk_size=50,
#    chunk_overlap=0
# )

# chunks = splitter.split_text(text)

# for i, chunk in enumerate(chunks):
#    print("Chunk", i+1)
#    print(chunk)
#    print("Length:", len(chunk))
#    print()

# ⸻
# 3️⃣ Now let’s simulate what happens internally
# Default separators:
# ["\n\n", "\n", " ", ""]
# Order matters.
# ⸻
# Step 1 — Try paragraph split (\n\n)
# Text splits into:
# Piece 1:
# Short para.
# Length:
# 11 chars ✔
# Good → becomes chunk.
# ⸻
# Piece 2:
# This is a very long paragraph that contains many many words and keeps going on and on to simulate a big paragraph which should exceed the chunk size limit so that we can observe how recursive splitting works internally.
# Length:
# ~220 chars ❌
# Too big.
# So recursive splitting starts.
# ⸻
# Piece 3:
# Another short para.
# Length:
# 20 chars ✔
# Good → becomes chunk.
# ⸻
# 4️⃣ Now recursive splitting starts on Piece 2
# Try next separator:
# \n
# But no newline inside.
# Still ~220 ❌
# Try next separator:
# space
# Now split into words:
# This
# is
# a
# very
# long
# paragraph
# that
# contains
# many
# many
# words
# ...

# ⸻
# 5️⃣ Now it starts building chunks under 50 chars
# It starts combining words:
# Build chunk:
# This is a very long paragraph that contains many
# Check length:
# ~48 ✔
# Chunk created.
# ⸻
# Next words:
# many words and keeps going on and on to simulate
# Length:
# ~47 ✔
# Chunk created.
# ⸻
# Next:
# a big paragraph which should exceed the chunk
# ~46 ✔
# Chunk created.
# ⸻
# Next:
# size limit so that we can observe how recursive
# ~48 ✔
# Chunk created.
# ⸻
# Next:
# splitting works internally.
# ~27 ✔
# Chunk created.
# ⸻
# 6️⃣ Final result becomes:
# Chunks:
# Chunk 1:
# Short para.
# Chunk 2:
# This is a very long paragraph that contains many
# Chunk 3:
# many words and keeps going on and on to simulate
# Chunk 4:
# a big paragraph which should exceed the chunk
# Chunk 5:
# size limit so that we can observe how recursive
# Chunk 6:
# splitting works internally.
# Chunk 7:
# Another short para.

# ⸻
# 7️⃣ What we learned from this example
# Recursive splitter did:
# Step 1:
# Split by paragraph.
# Short ✔
# Long ❌
# Short ✔
# Step 2:
# Long paragraph too big → try next separator.
# Step 3:
# Split by words.
# Step 4:
# Combine words until chunk_size reached.
# ⸻
# 8️⃣ The key insight (this is the main point)
# Recursive splitter tries:
# Keep structure if possible
# But if structure violates chunk_size:
# Break structure gradually
# Meaning priority:
# Structure first
# Size enforcement second

# ⸻
# 9️⃣ Now let’s add overlap example
# If:
# chunk_overlap = 10
# Then:
# Chunk2:
# This is a very long paragraph that contains many
# Chunk3 would start with last 10 chars:
# Example:
# tains many words and keeps going on and on
# Overlap part:
# tains many
# Repeated.
# ⸻
# 🔟 The simplest mental model (best way to remember)
# Recursive splitter behaves like:
# “I will try to cut nicely.
# But if text is too big,
# I will cut more aggressively.”




### Token text splitter
token_text_splitter = TokenTextSplitter(chunk_size=100, chunk_overlap=20)
token_chunk = token_text_splitter.split_text(text)
print(len(token_chunk))
# print(f"first Chunk: {token_chunk[0][:100]}...")


loaders
from langchain_community.document_loaders import PyPDFLoader
loader = PyPDFLoader("C:\\Users\\2000137378\\Desktop\\newproject\\RAG_PIPELINES\\quote_20260325_114738.pdf")
document = loader.load()
print(document)







# 1️⃣ First understand why multiple PDF loaders exist
# PDF is a very messy format internally.
# PDF does NOT store text like:
# Line 1
# Line 2
# Instead it stores:
# Text position
# Font
# Coordinates
# Objects
# Drawing instructions
# So extracting text is hard.
# Different libraries solve this differently.
# That is why multiple loaders exist.
# ⸻
# 2️⃣ PyPDFLoader (simplest)
# Uses:
# pypdf library
# Basic text extraction.
# Example:
# from langchain_community.document_loaders import PyPDFLoader
# loader = PyPDFLoader("sample.pdf")
# docs = loader.load()
# print(docs[0].page_content)

# ⸻
# How PyPDFLoader works internally
# Process:
# PDF
# ↓
# pypdf reads page
# ↓
# Extract text
# ↓
# Create Document per page

# ⸻
# Advantages:
# ✔ Simple
# ✔ Lightweight
# ✔ Fast
# ✔ Good for clean PDFs
# ⸻
# Problems:
# ❌ Bad with tables
# ❌ Bad with complex layouts
# ❌ Sometimes broken text order
# Example bad extraction:
# Instead of:
# Name Age Policy
# Ali   30  Health
# You may get:
# Name Policy Age Health 30 Ali
# Layout lost.
# ⸻
# 3️⃣ PyMuPDFLoader (better extraction)
# Uses:
# PyMuPDF (fitz)
# Much better parser.
# Example:
# from langchain_community.document_loaders import PyMuPDFLoader
# loader = PyMuPDFLoader("sample.pdf")
# docs = loader.load()

# ⸻
# Internal process:
# PDF
# ↓
# PyMuPDF parsing engine
# ↓
# Better layout detection
# ↓
# Document per page

# ⸻
# Advantages:
# ✔ Better text order
# ✔ Faster than pypdf
# ✔ Handles layouts better
# ✔ Good for production
# ⸻
# Problems:
# ❌ Still struggles with complex tables
# ❌ No semantic structure understanding
# ⸻
# 4️⃣ UnstructuredPDFLoader (most advanced)
# Uses:
# Unstructured library
# This is different.
# It tries to understand:
# Document structure
# Not just text.
# Example:
# It detects:
# Title
# Paragraph
# Table
# Header
# Footer
# Lists
# Example:
# from langchain_community.document_loaders import UnstructuredPDFLoader
# loader = UnstructuredPDFLoader("sample.pdf")
# docs = loader.load()

# ⸻
# Internal process:
# PDF
# ↓
# Unstructured parser
# ↓
# Layout detection
# ↓
# Element detection
# ↓
# Document objects

# ⸻
# Advantages:
# ✔ Best text quality
# ✔ Detects sections
# ✔ Detects tables
# ✔ Detects headings
# ✔ Best for RAG ingestion
# ⸻
# Problems:
# ❌ Slower
# ❌ More dependencies
# ❌ Heavier install
# ⸻
# 5️⃣ Side-by-side comparison
# Loader Speed Quality Best use
# PyPDFLoader Fast Basic Simple PDFs
# PyMuPDFLoader Fast Good Most RAG systems
# UnstructuredPDFLoader Slower Best Complex documents

# ⸻
# 6️⃣ Real engineer recommendation
# Most RAG engineers choose:
# Default choice:
# PyMuPDFLoader
# Because:
# Good balance of speed + quality.
# ⸻
# If document complex:
# UnstructuredPDFLoader
# Example:
# Insurance documents
# Research papers
# Contracts
# Reports
# ⸻
# Rarely recommended:
# PyPDFLoader
# Only if simple PDF.
# ⸻
# 7️⃣ Important real world insight (this matters a lot)
# Loader choice affects:
# Extraction quality
#     ↓
# Chunk quality
#     ↓
# Embedding quality
#     ↓
# Retrieval quality
#     ↓
# LLM answers
# Bad loader → bad RAG.
# Most beginners ignore this.
# ⸻
# 8️⃣ Example difference (real effect)
# Suppose policy says:
# Coverage amount: 50000 USD
# Bad loader may produce:
# Coverage USD amount 50000
# Embedding meaning changes.
# Retrieval suffers.
# ⸻
# 9️⃣ What companies actually use
# Production ingestion often uses:
# Unstructured
# PyMuPDF
# PDFPlumber
# Azure Document Intelligence
# For enterprise data.
# ⸻
# 🔟 Simple decision rule
# Use this:
# If learning:
# PyPDFLoader
# If building RAG:
# PyMuPDFLoader
# If enterprise docs:
# UnstructuredPDFLoader

# ⸻
# 11️⃣ One line summary
# All three load PDFs but differ in text extraction quality, not functionality.
# They all produce:
# List[Document]
# But content quality differs.



embedding
from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings
from langchain_community.document_loaders import TextLoader
from langchain_text_splitters import CharacterTextSplitter, RecursiveCharacterTextSplitter
from dotenv import load_dotenv
from langchain_core.documents import Document
import os

###split_text=--------------------------------------------------------------
load_dotenv()

# embeddings = AzureOpenAIEmbeddings(
#     azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),
#     openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
#     openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
#     azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
#     chunk_size=1024  # Adding required chunk_size parameter
# )

# text_splitter = RecursiveCharacterTextSplitter(
#     chunk_size=int(os.getenv("CHUNK_SIZE", "800")),
#     chunk_overlap=int(os.getenv("CHUNK_OVERLAP", "100")),
#     length_function=len,
# )

# loader = TextLoader("C:\\Users\\2000137378\\Desktop\\newproject\\RAG_PIPELINES\\mytext.txt", encoding="utf-8")
# documents = loader.load() #THIS GIVES US A LIST OF DOCUMENT OBJECTS BUT WE NEED STRING AS SPLIT_TEXT EXPECTS A STRING NOT A DOCUMENT OBJECT
# document_text = documents[0].page_content #WE EXTRACT THE STRING FROM THE DOCUMENT OBJECT TO PASS IT TO SPLIT_TEXT

# chunks = text_splitter.split_text(document_text) #THIS GIVES US A LIST OF STRINGS (CHUNKS) WHICH WE CAN THEN CONVERT TO DOCUMENT OBJECTS TO PASS TO CHROMA

# my_dir = os.path.dirname(os.path.abspath(__file__))
# print(f"Current directory: {my_dir}")


# Now we have chunks as a list of strings. We need to convert them to Document objects to pass to Chroma.

# docs = []
# for chunk in chunks:
#     docs.append(Document(page_content=chunk))

# vectorstore = Chroma.from_documents(
#     # documents=chunks, #WE CANNOT PASS THE LIST OF STRINGS (CHUNKS) TO CHROMA BECAUSE IT EXPECTS A LIST OF DOCUMENT OBJECTS. WE NEED TO CONVERT THE CHUNKS TO DOCUMENT OBJECTS FIRST.
#     documents=docs,   #WE PASS THE LIST OF DOCUMENT OBJECTS TO CHROMA
#     embedding=embeddings,
#     collection_name="my_collection",
#     persist_directory=my_dir
#     )





###------------------SPLIT DOCUMENT

#below is the exact above code this time using SplitDocuments instead of split_text to directly get Document objects instead of strings which we then pass to Chroma without needing to convert them to Document objects again.

load_dotenv()
embeddings = AzureOpenAIEmbeddings(
    azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),
    openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
    openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
    azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
    chunk_size=1024  # Adding required chunk_size parameter
)
text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=int(os.getenv("CHUNK_SIZE", "800")),
    chunk_overlap=int(os.getenv("CHUNK_OVERLAP", "100")),
    length_function=len,
)
loader = TextLoader("C:\\Users\\2000137378\\Desktop\\newproject\\RAG_PIPELINES\\mytext.txt", encoding="utf-8")
documents = loader.load() #THIS GIVES US A LIST OF DOCUMENT OBJECTS BUT WE NEED
    # TO SPLIT THEM INTO CHUNKS AS DOCUMENT OBJECTS TO PASS TO CHROMA
chunks = text_splitter.split_documents(documents)
# my_dir = os.path.dirname(os.path.abspath(__file__))
my_dir = os.path.join(os.path.dirname(os.path.abspath(__file__)), "input")

vectorstore = Chroma.from_documents(
    documents=chunks, #NOW WE CAN PASS THE CHUNKS DIRECTLY TO CHROMA BECAUSE THEY ARE ALREADY DOCUMENT OBJECTS
    embedding=embeddings,
    collection_name="my_collection",
    persist_directory=my_dir
    )


print(len(chunks))
# print(f"first Chunk: {chunks[0][:100]}...")
print(f"first Chunk: {chunks[0].page_content[:100]}...")

retrievers

from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings
from dotenv import load_dotenv
import os

load_dotenv()

embeddings = AzureOpenAIEmbeddings(
    azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),
    openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
    openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
    azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),      
    # chunk_size=1024  # Adding required chunk_size parameter
)


my_dir = os.path.join(os.path.dirname(os.path.abspath(__file__)), "input")

vectorstore = Chroma(
    collection_name="my_collection",
    embedding_function=embeddings,
    persist_directory=my_dir
)

query = input("Enter your query: ")

results = vectorstore.similarity_search(query, k=3)

print("Top 3 similar documents:")

for doc in results:
    print(doc.page_content)
    print("-----")



from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings
from dotenv import load_dotenv
import os

import logging
from typing import Dict, Any, List
# from langchain.chains import LLMChain
from langchain_classic.chains.llm import LLMChain
# from langchain.prompts import PromptTemplate
from langchain_core.prompts import PromptTemplate
# from langchain_community.chat_models import AzureChatOpenAI
from langchain_openai import AzureChatOpenAI, AzureOpenAIEmbeddings
# from langchain_community.vectorstores import Chroma
from langchain_chroma import Chroma
# from langchain_community.embeddings import AzureOpenAIEmbeddings
# from azure.storage.blob import BlobServiceClient, ContainerClient
from azure.core.exceptions import ResourceExistsError
import re
# from datetime import datetime
# from urllib.parse import urlparse, unquote
# from Jarvis.MCP.common.phoenix_prompt import build_prompt

import asyncio
# Load environment variables
load_dotenv()

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# Initialize Azure OpenAI embeddings
embeddings = AzureOpenAIEmbeddings(
    azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),
    openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
    openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
    azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
    chunk_size=1024
)


def query_documents(vectordb: Any, user_query: str) -> str:
    """
    Answer user questions by querying vectorized document content.
    
    Performs semantic search on the vectorized content to find relevant chunks,
    then uses LLM to generate a direct answer based on the context.
    
    Args:
        vectordb: Loaded ChromaDB instance with embeddings
        user_query: User's question as a string
    
    Returns:
        Direct answer string (not structured JSON)
    """
    print(f"🔍 Processing query: {user_query}")
    
    llm = AzureChatOpenAI(
        openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
        azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
        openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
        deployment_name=os.getenv("AZURE_OPENAI_CHAT_DEPLOYMENT"),
        temperature=0,
        top_p=1
    )
    
    # Perform semantic search with large k value for better context
    print(f"📚 Performing semantic search...")
    relevant_chunks = vectordb.similarity_search(
        user_query,
        k=5  # Large k for comprehensive context
    )
    
    if not relevant_chunks:
        print("⚠️ No relevant chunks found in the document")
        return "I could not find any relevant information in the document to answer your question."
    
    # Combine relevant chunks into context
    context = "\n".join([chunk.page_content for chunk in relevant_chunks])
    print("This is my context",context)
    # Create prompt for question answering
    qa_prompt = PromptTemplate(
        input_variables=["context", "question"],
        template="""You are an expert assistant for answering questions about documents.

Based on the provided document context, answer the user's question accurately and concisely.

QUESTION: {question}

CONTEXT:
{context}

ANSWER:
Provide a clear, direct answer based only on the context provided. 
If the answer is not found in the context, explicitly state "Not found in documents."
Do not guess or use external knowledge.
Keep your answer concise and relevant to the question."""
    )
    
    chain = LLMChain(llm=llm, prompt=qa_prompt)
    
    try:
        print(f"Generating answer...")
        answer = chain.run({
            "context": context,
            "question": user_query
        }).strip()
        
        print(f"Answer generated successfully")
        return answer
        
    except Exception as e:
        logger.error(f"Error generating answer: {e}")
        return f"Error processing your query: {str(e)}"


async def process_query_retrieval(vector_db_path: str, query: str) -> Dict[str, Any]:
    """
    Main processing function for query-based document retrieval.
    
    Takes a user query, performs semantic search on vectorized content,
    and returns a direct answer without JSON extraction.
    
    Args:
        vector_db_path: Can be either:
            1. Local absolute path from pdf_vectorizer output: /home/jarvis/rohan/Document_Query_MCP/data/vec_db/pdf_name
            2. Blob URL (fallback): https://... - will extract pdf name and construct local path
        query: User's question as a string
    
    Returns:
        Dict with answer and metadata
    """
    try:
        # Handle both blob URLs and local paths
        # if vector_db_path.startswith(("http://", "https://")):
            # This is a blob URL - extract the PDF name and construct local path
        #     logger.warning(f"Received blob URL instead of local path. Extracting PDF name...")
        #     path = unquote(urlparse(vector_db_path).path)
        #     file_name_with_ext = os.path.basename(path)
        #     pdf_name = os.path.splitext(file_name_with_ext)[0]
            
        #     # Construct the local vector_db_path
        #     vdb_base_path = os.getenv("QUERY_VDB_PATH", "./data/vec_db")
        #     vector_db_path = os.path.abspath(os.path.join(vdb_base_path, pdf_name))
        #     logger.info(f"Reconstructed local vector_db_path: {vector_db_path}")
        # else:
            # Ensure it's an absolute path
        vector_db_path = os.path.abspath(vector_db_path)
        
        # Get safe collection name from vector_db_path
        # ChromaDB only allows: [a-zA-Z0-9._-]
        # safe_name = os.path.basename(vector_db_path).lower()
        # safe_name = safe_name.replace(" ", "_").replace("-", "_").replace(".", "_")
        # # Remove parentheses and other special characters
        # safe_name = "".join(c if c.isalnum() or c in "._-" else "" for c in safe_name)
        # # Ensure it starts and ends with alphanumeric
        # safe_name = safe_name.strip("._-")
        # collection_name = f"{safe_name}_collection"
        # persist_directory = os.path.join(vector_db_path, "chroma_db", safe_name)
        
        # Verify the vector DB exists
        if not os.path.exists(vector_db_path):
            raise FileNotFoundError(f"Vector database not found at: {vector_db_path}")
        
        # Load the vector store
        print(f"📚 Loading vector store from {vector_db_path}")
        vectordb = Chroma(
            embedding_function=embeddings,
            persist_directory=vector_db_path,
            collection_name="newbig_collection"
        )
        
        # Get answer using semantic search and LLM
        answer = query_documents(vectordb, query)
        # Get PDF name for metadata
        pdf_name = os.path.basename(vector_db_path)
        
        return {
            "answer": answer,
            "metadata": {
                "is_error": False,
                "error_message": None,
                "query": query,
                "source_pdf": pdf_name,
                "vector_db_path": vector_db_path
            }
        }
        
    except Exception as e:
        logger.exception("Error in query retrieval process")
        return {
            "answer": None,
            "metadata": {
                "is_error": True,
                "error_message": str(e),
                "query": query,
                "source_pdf": None,
                "vector_db_path": vector_db_path if 'vector_db_path' in locals() else None
            }
        }





# if __name__ == "__main__":
#     await process_query_retrieval("C:\\Users\\2000137378\\Desktop\\newproject\\RAG_PIPELINES\\input","What is Contact Number")
    
if __name__ == "__main__":
    result = asyncio.run(process_query_retrieval(
        r"C:\Users\2000137378\Desktop\newproject\RAG_PIPELINES\big_input",
        "whose number is this +64 21 555 0198"
    ))
    print(result)    

metadatafilterretrieval

from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings
from langchain_community.document_loaders import TextLoader
from langchain_text_splitters import RecursiveCharacterTextSplitter
from dotenv import load_dotenv
import os
load_dotenv()
# Embedding model
embeddings = AzureOpenAIEmbeddings(
   azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),
   openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
   openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
   azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
   chunk_size=1024
)
# Text splitter
text_splitter = RecursiveCharacterTextSplitter(
   chunk_size=800,
   chunk_overlap=100
)
# Load document
loader = TextLoader(
   r"C:\Users\2000137378\Desktop\newproject\RAG_PIPELINES\mytext.txt",
   encoding="utf-8"
)
documents = loader.load()
# Split into chunks
chunks = text_splitter.split_documents(documents)
# ADD METADATA HERE
for chunk in chunks:
   chunk.metadata["doc_type"] = "insurance"
   chunk.metadata["category"] = "underwriting"
   chunk.metadata["source_file"] = "mytext.txt"

# Directory to store vectors
my_dir = os.path.join(
   os.path.dirname(os.path.abspath(__file__)),
   "input8.1"
)
# Create vector DB
vectorstore = Chroma.from_documents(
   documents=chunks,
   embedding=embeddings,
   collection_name="mynew_collection",
   persist_directory=my_dir
)
print("\nTotal chunks:",len(chunks))
print("\nFirst chunk text:")
print(chunks[0].page_content[:150])
print("\nFirst chunk metadata:")
print(chunks[0].metadata)



metadatafilteringretrival
from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings, AzureChatOpenAI
from langchain_core.prompts import PromptTemplate
from langchain_classic.chains.llm import LLMChain
from dotenv import load_dotenv
import os
import asyncio
load_dotenv()
# Embeddings
embeddings = AzureOpenAIEmbeddings(
   azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),
   openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
   openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
   azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
   chunk_size=1024
)

def query_documents(vectordb, user_query):
   print("\nProcessing query:",user_query)
   llm = AzureChatOpenAI(
       openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
       azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
       openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
       deployment_name=os.getenv("AZURE_OPENAI_CHAT_DEPLOYMENT"),
       temperature=0
   )
   print("\nPerforming metadata filtered search...")
   # METADATA FILTER HERE
   relevant_chunks = vectordb.similarity_search(
       user_query,
       k=5,
       filter={"doc_type":"insurance"}
   )
   print("\nChunks retrieved:",len(relevant_chunks))
   if not relevant_chunks:
       return "No relevant information found"
   # Print metadata to confirm filtering
   print("\nMetadata of retrieved chunks:")
   for chunk in relevant_chunks:
       print(chunk.metadata)
   # Build context
   context = "\n".join(
       chunk.page_content for chunk in relevant_chunks
   )
   # Prompt
   qa_prompt = PromptTemplate(
       input_variables=["context","question"],
       template="""
Answer the question using only the context.
QUESTION:
{question}
CONTEXT:
{context}
ANSWER:
"""
   )
   chain = LLMChain(
       llm=llm,
       prompt=qa_prompt
   )
   answer = chain.run({
       "context":context,
       "question":user_query
   })
   return answer

async def process_query():
   vector_db_path = r"C:\Users\2000137378\Desktop\newproject\RAG_PIPELINES\big_input"
   vectordb = Chroma(
       embedding_function=embeddings,
       persist_directory=vector_db_path,
       collection_name="big_collection"
   )
   answer = query_documents(
       vectordb,
       "whose number is this +64 21 555 0198"
   )
   print("\nFinal Answer:\n")
   print(answer)

if __name__ == "__main__":
   asyncio.run(process_query())


metadafilterloader
from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings
from langchain_community.document_loaders import TextLoader
from langchain_text_splitters import RecursiveCharacterTextSplitter
from dotenv import load_dotenv
import os
load_dotenv()
# ================= EMBEDDINGS =================
embeddings = AzureOpenAIEmbeddings(
   azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),
   openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
   openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
   azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
   chunk_size=1024
)
# ================= SPLITTER =================
text_splitter = RecursiveCharacterTextSplitter(
   chunk_size=800,
   chunk_overlap=100
)
# ================= LOADER =================
loader = TextLoader(
   r"C:\Users\2000137378\Desktop\newproject\RAG_PIPELINES\mytext.txt",
   encoding="utf-8"
)
documents = loader.load()
# ================= CHUNKING =================
chunks = text_splitter.split_documents(documents)
# ================= AUTO METADATA TAGGING =================
for chunk in chunks:
   text = chunk.page_content.lower()
   chunk.metadata["doc_type"] = "insurance"
   chunk.metadata["source_file"] = "mytext.txt"
   # SECTION DETECTION
   if "property overview" in text or "year built" in text:
       chunk.metadata["section"] = "property"
   elif "claims history" in text or "amount paid" in text:
       chunk.metadata["section"] = "claims"
   elif "construction" in text:
       chunk.metadata["section"] = "construction"
   elif "security features" in text:
       chunk.metadata["section"] = "security"
   elif "policyholder" in text:
       chunk.metadata["section"] = "policyholder"
   else:
       chunk.metadata["section"] = "general"
# ================= VECTOR DB =================
my_dir = os.path.join(
   os.path.dirname(os.path.abspath(__file__)),
   "input9.1"
)
vectorstore = Chroma.from_documents(
   documents=chunks,
   embedding=embeddings,
   collection_name="my9.1_collection",
   persist_directory=my_dir
)
# ================= DEBUG =================
print("\nTotal chunks:",len(chunks))
print("\nExample chunk:")
print(chunks[0].page_content[:150])
print("\nMetadata:")
print(chunks[0].metadata)



metadatafilteringquery
from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings, AzureChatOpenAI
from langchain_core.prompts import PromptTemplate
from langchain_classic.chains.llm import LLMChain
from dotenv import load_dotenv
import os
load_dotenv()
# ================= EMBEDDINGS =================
embeddings = AzureOpenAIEmbeddings(
   azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),
   openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
   openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
   azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
   chunk_size=1024
)
# ================= LOAD VECTOR DB =================
vector_db_path = r"C:\Users\2000137378\Desktop\newproject\RAG_PIPELINES\big_input"
vectordb = Chroma(
   embedding_function=embeddings,
   persist_directory=vector_db_path,
   collection_name="my_collection"
)
# ================= QUERY =================
user_query = "+64 21 555 0198"
# CHANGE SECTION TO TEST
section_filter="general"
print("\nQuery:",user_query)
print("\nFiltering section:",section_filter)
# ================= FILTERED SEARCH =================
relevant_chunks = vectordb.similarity_search(
   user_query,
   k=5,
   filter={
      "$and":[ {"doc_type":"insurance"},
       {"section":section_filter}
   ]
   }
)
print("\nChunks found:",len(relevant_chunks))
# ================= SHOW RETRIEVED =================
for chunk in relevant_chunks:
   print("\nMetadata:",chunk.metadata)
   print("Text:",chunk.page_content[:150])
# ================= BUILD CONTEXT =================
context="\n".join(
   chunk.page_content for chunk in relevant_chunks
)
# ================= LLM =================
llm = AzureChatOpenAI(
   openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
   azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
   openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
   deployment_name=os.getenv("AZURE_OPENAI_CHAT_DEPLOYMENT"),
   temperature=0
)
prompt=PromptTemplate(
   input_variables=["context","question"],
   template="""
Answer only from context.
QUESTION:
{question}
CONTEXT:
{context}
ANSWER:
"""
)
chain=LLMChain(
   llm=llm,
   prompt=prompt
)
answer=chain.run({
   "context":context,
   "question":user_query
})
print("\nFINAL ANSWER:\n")
print(answer)


metadatadynamicfiltering
# (venv) PS C:\Users\2000137378\Desktop\newproject\RAG_PIPELINES> python .\9.1metadatafilteringloader.py


from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings, AzureChatOpenAI
from langchain_core.prompts import PromptTemplate
from langchain_classic.chains.llm import LLMChain
from dotenv import load_dotenv
import os
load_dotenv()
# ================= EMBEDDINGS =================
embeddings = AzureOpenAIEmbeddings(
   azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),
   openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
   openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
   azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT")
)
# ================= LOAD VECTOR DB =================
vector_db_path = r"C:\Users\2000137378\Desktop\newproject\RAG_PIPELINES\big_input"
vectordb = Chroma(
   embedding_function=embeddings,
   persist_directory=vector_db_path,
   collection_name="big_collection"
)
# ================= LLM =================
llm = AzureChatOpenAI(
   openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
   azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
   openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
   deployment_name=os.getenv("AZURE_OPENAI_CHAT_DEPLOYMENT"),
   temperature=0
)
# ================= USER QUERY =================
user_query="whose number is this +64 21 555 0198"
print("\nUser Query:",user_query)
# ================= SECTION DETECTOR =================
section_prompt = PromptTemplate(
   input_variables=["question"],
   template="""
You are a document query classifier.
Classify which section this question belongs to.
Possible sections:
property
claims
construction
security
policyholder
general
QUESTION:
{question}
Return ONLY the section name.
"""
)

section_chain = LLMChain(
   llm=llm,
   prompt=section_prompt
)

section = section_chain.run({
   "question":user_query
}).strip().lower()
print("\nDetected section:",section)
# ================= RETRIEVAL =================

relevant_chunks = vectordb.similarity_search(
   user_query,
   k=5,
#    filter={
#        "doc_type":"insurance",
#        "section":section
#    }


  filter={
      "$and":[ {"doc_type":"insurance"},
       {"section":section}
   ]
    }
)

print("\nChunks retrieved:",len(relevant_chunks))
# Debug retrieved chunks
for chunk in relevant_chunks:
   print("\nMetadata:",chunk.metadata)
   print("Text:",chunk.page_content[:120])
# ================= BUILD CONTEXT =================
context="\n".join(
   chunk.page_content for chunk in relevant_chunks
)
# ================= QA PROMPT =================
qa_prompt=PromptTemplate(
   input_variables=["context","question"],
   template="""
Answer using ONLY the context.
QUESTION:
{question}
CONTEXT:
{context}
ANSWER:
"""
)
qa_chain=LLMChain(
   llm=llm,
   prompt=qa_prompt
)
answer=qa_chain.run({
   "context":context,
   "question":user_query
})
print("\nFINAL ANSWER:\n")
print(answer)


hybridretrieval
from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings, AzureChatOpenAI
from langchain_core.prompts import PromptTemplate
from langchain_classic.chains.llm import LLMChain
from dotenv import load_dotenv
import os

load_dotenv()

# ================= EMBEDDINGS =================

embeddings = AzureOpenAIEmbeddings(

    azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),

    openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),

    openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),

    azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT")
)

# ================= VECTOR DB =================

vector_db_path=r"C:\Users\2000137378\Desktop\newproject\RAG_PIPELINES\big_input"

vectordb=Chroma(

    embedding_function=embeddings,

    persist_directory=vector_db_path,

    collection_name="big_collection"
)

# ================= LLM =================

llm=AzureChatOpenAI(

    openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),

    azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),

    openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),

    deployment_name=os.getenv("AZURE_OPENAI_CHAT_DEPLOYMENT"),

    temperature=0
)

# ================= USER QUERY =================

user_query="whose number is this +64 21 555 0198"

print("\nUser Query:",user_query)

# ================= VECTOR SEARCH =================

print("\nRunning vector search...")

vector_results=vectordb.similarity_search(

    user_query,

    k=3
)

# ================= KEYWORD SEARCH =================

print("\nRunning keyword search...")

all_docs=vectordb.get()

keyword_results=[]

for text,metadata in zip(

        all_docs["documents"],
        all_docs["metadatas"]
):

    if user_query.lower() in text.lower():

        keyword_results.append(text)

# ================= MERGE RESULTS =================

combined_texts=[]

# Add vector results
for doc in vector_results:

    combined_texts.append(doc.page_content)

# Add keyword results
for text in keyword_results:

    if text not in combined_texts:

        combined_texts.append(text)

print("\nTotal combined chunks:",len(combined_texts))

# ================= CONTEXT =================

context="\n".join(combined_texts)

# ================= QA PROMPT =================

qa_prompt=PromptTemplate(

    input_variables=["context","question"],

    template="""
Answer using ONLY this context.

QUESTION:
{question}

CONTEXT:
{context}

ANSWER:
"""
)

qa_chain=LLMChain(

    llm=llm,

    prompt=qa_prompt
)

answer=qa_chain.invoke({

    "context":context,

    "question":user_query

})["text"]

# ================= ANSWER =================

print("\nFINAL ANSWER:\n")

print(answer)


embedding my querytopdf
from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings
from langchain_community.document_loaders import TextLoader
from langchain_text_splitters import RecursiveCharacterTextSplitter
from dotenv import load_dotenv
import os
import time

load_dotenv()

# ================= EMBEDDINGS =================

embeddings = AzureOpenAIEmbeddings(

    azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),

    openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),

    openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),

    azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),

    chunk_size=50,          # IMPORTANT → smaller batch

    max_retries=10          # retry if rate limited
)

# ================= TEXT SPLITTER =================

text_splitter = RecursiveCharacterTextSplitter(

    chunk_size=800,

    chunk_overlap=100
)

# ================= LOAD LARGE FILE =================

loader = TextLoader(

    r"C:\Users\2000137378\Desktop\newproject\RAG_PIPELINES\complex_realistic_corpus.txt",

    encoding="utf-8"
)

documents = loader.load()

print("\nDocuments loaded:",len(documents))

# ================= SPLIT =================

chunks = text_splitter.split_documents(documents)

print("\nTotal chunks:",len(chunks))

# ================= VECTOR DIRECTORY =================

persist_dir = os.path.join(

    os.path.dirname(os.path.abspath(__file__)),

    "big_input"
)

# ================= CREATE EMPTY DB =================

vectordb = Chroma(

    embedding_function=embeddings,

    persist_directory=persist_dir,

    collection_name="newbig_collection"
)

# ================= BATCH INGESTION =================

batch_size = 50

print("\nStarting batched ingestion...")

for i in range(0,len(chunks),batch_size):

    batch = chunks[i:i+batch_size]

    try:

        vectordb.add_documents(batch)

        print(f"Inserted batch {i} → {i+batch_size}")

        # IMPORTANT → prevent rate limit
        time.sleep(1)

    except Exception as e:

        print("Retrying batch due to:",e)

        time.sleep(10)

        vectordb.add_documents(batch)

print("\nIngestion complete")

print("\nVector DB stored at:",persist_dir)


import random

names=[
"Alex Taylor","Jordan Smith","Chris Morgan",
"Taylor Holdings","Morgan Risk Ltd",
"Pacific Underwriters","Summit Brokers"
]

phones=[
"+64 21 555 0198",
"021-555-0198",
"64 21 5550198",
"+64 (21) 555 0198",
"0215550198"
]

zips=[
"95123","94107","10001","400001","20005"
]

years=[
"1998","2001","2005","2010","2015"
]

noise_blocks=[

"""
Inspection notes:
Minor wear observed.
Previous claim ref CL8821.
Surveyor comments archived.
""",

"""
Maintenance log fragment:
Roof inspection passed.
Drainage acceptable.
Engineer reference ENG9921.
""",

"""
Broker internal note:
Client prefers digital comms.
Risk appetite moderate.
Underwriting review pending.
""",

"""
Document appendix:
Photos archived separately.
Structural review recommended.
Policy subject to renewal review.
"""
]

formats=[

lambda n,p,z,y: f"""
INSURANCE SUBMISSION

Client: {n}

Contact pathways:
Primary mobile -> {p}

Construction metadata:
Property likely built circa {y}

Geographic code:
ZIP:{z}
""",

lambda n,p,z,y: f"""
CLIENT DATA RECORD

Holder : {n}

Reach at:
Tel number {p}

Asset notes:
Construction year believed {y}

Postal marker {z}
""",

lambda n,p,z,y: f"""
UNDERWRITING SNAPSHOT

Entity name {n}

Communication:
Mobile contact = {p}

Risk notes:
Dwelling erected approx {y}

Location identifier {z}
""",

lambda n,p,z,y: f"""
RISK REPORT

Policy customer {n}

Phone listing:
{p}

Asset built year {y}

Area code {z}
"""
]

output="complex_realistic_corpus.txt"

with open(output,"w",encoding="utf-8") as f:

    for i in range(15000):

        name=random.choice(names)+"_"+str(i)

        phone=random.choice(phones)

        zipc=random.choice(zips)

        year=random.choice(years)

        template=random.choice(formats)

        doc=template(name,phone,zipc,year)

        doc+=random.choice(noise_blocks)

        # Add random garbage lines
        if random.random()>0.6:

            doc+=f"\nReference ID: REF{i}{random.randint(100,999)}"

        if random.random()>0.5:

            doc+=f"\nInternal code ZX-{random.randint(1000,9999)}"

        f.write(doc+"\n\n")

print("Complex corpus generated")



creatingdatasettoseewhyhybrid
import random
import os

base_text = """
Home Insurance Underwriting Submission Report

Policyholder: Alex Taylor
Contact Number: +64 21 555 0198
Email: alex.taylor@example.co.nz

Property Overview:
Year Built: 1998
Construction: Timber frame
Roof: Colorsteel
Floors: 2

Address:
1234 Maple Ridge Lane
San Jose
California
Zip: 95123

Broker:
Casey Morgan
Harbour Risk Advisory

Claim History:
Water damage 2022
Amount: 2480

"""

cities = [
"San Jose","Auckland","Sydney","London",
"Toronto","Melbourne","Dubai","Mumbai"
]

years=[1990,1995,1998,2001,2005,2010,2015,2020]

zipcodes=["95123","94107","10001","20005","400001"]

phones=[
"+64 21 555 0198",
"+61 488 555 111",
"+1 408 777 222",
"+91 98765 43210"
]

output_file="big_insurance_dataset.txt"

with open(output_file,"w",encoding="utf-8") as f:

    for i in range(20000):   # adjust number here

        text=base_text

        text=text.replace(
            "Alex Taylor",
            "Client_"+str(i)
        )

        text=text.replace(
            "1998",
            str(random.choice(years))
        )

        text=text.replace(
            "San Jose",
            random.choice(cities)
        )

        text=text.replace(
            "95123",
            random.choice(zipcodes)
        )

        text=text.replace(
            "+64 21 555 0198",
            random.choice(phones)
        )

        f.write(text+"\n\n")

print("Dataset generated")





1notes.txt
Retrieval just means:
Find relevant context before asking LLM
There are 5 main retrieval approaches engineers use:
1 Similarity search (you know this)
2 MMR retrieval (diverse retrieval)
3 Metadata filtering
4 Hybrid search (vector + keyword)
5 Multi-query retrieval
These cover almost all real RAG systems.
Let’s go one by one.
⸻
2️⃣ Similarity Search (what you already use)
Concept:
Query → embedding → compare vectors → top matches
Code:
results = vectordb.similarity_search(
   query,
   k=5
)
This uses:
Cosine similarity.
Problem:
May return similar chunks that repeat same info.
Example:
5 chunks about same paragraph.
Not diverse.
⸻
3️⃣ MMR Retrieval (Max Marginal Relevance)
This is very important and used in production.
Concept:
Instead of:
Most similar chunks
MMR finds:
Similar + diverse chunks
Meaning:
Avoid duplicates.
Better coverage.
⸻
Example problem similarity search causes:
Query:
House construction details
Similarity may return:
Construction paragraph 1
Construction paragraph 2
Construction paragraph 3
Construction paragraph 4
Construction paragraph 5
All same topic.
Bad coverage.
⸻
MMR instead returns:
Construction paragraph
Roof paragraph
Foundation paragraph
Renovation paragraph
Materials paragraph
Better context.
⸻
MMR code
results = vectordb.max_marginal_relevance_search(
   query,
   k=5,
   fetch_k=20
)
Meaning:
Fetch 20 candidates.
Return best 5 diverse ones.
⸻
Using retriever interface:
retriever = vectordb.as_retriever(
   search_type="mmr",
   search_kwargs={
       "k":5,
       "fetch_k":20
   }
)
results = retriever.invoke(query)

⸻
4️⃣ Metadata Filtering (very important)
Instead of searching whole DB.
Search subset.
Example:
Only property section.
Example metadata:
Document(
page_content="Year built 1998",
metadata={"section":"property"}
)
Query:
Only property:
results = vectordb.similarity_search(
   query,
   filter={"section":"property"}
)
This is used heavily in enterprise RAG.
Example use:
Filter:
policy type
document type
customer id
date
region

⸻
5️⃣ Hybrid Search (vector + keyword)
Similarity search misses exact matches sometimes.
Example:
Query:
Policy ABC123
Embedding may miss.
Keyword search finds instantly.
Hybrid combines:
Vector search + BM25 keyword search
LangChain example:
retriever = vectordb.as_retriever(
   search_type="similarity_score_threshold",
   search_kwargs={
       "score_threshold":0.5
   }
)
Or using hybrid retrievers (advanced).
Most enterprise RAG uses hybrid.
⸻
6️⃣ Multi Query Retrieval (very powerful)
Instead of 1 query:
Generate multiple query versions.
Example:
User asks:
Year built
LLM generates:
construction year
house built date
property age
year of construction
Then searches all.
Improves recall.
⸻
Code example:
from langchain.retrievers.multi_query import MultiQueryRetriever
retriever = MultiQueryRetriever.from_llm(
   vectordb.as_retriever(),
   llm
)
results = retriever.invoke(query)
This improves retrieval accuracy significantly.
⸻
7️⃣ Parent Document Retriever (advanced RAG)
Problem:
Small chunks lose context.
Solution:
Store:
Small chunks → retrieve → return larger parent doc.
Example:
Store:
200 char chunks.
Return:
Full section.
Concept:
Chunk retrieval
      ↓
Map to parent document
      ↓
Return full context
Used in enterprise RAG.
⸻
8️⃣ Contextual Compression Retriever
Instead of returning raw chunks:
LLM compresses chunks.
Example:
Chunk:
Large paragraph
Retriever returns:
Only relevant sentences.
Code:
from langchain.retrievers import ContextualCompressionRetriever
This is advanced optimization.
⸻
9️⃣ Real RAG retrieval strategies engineers choose
Most real systems choose:
Basic RAG:
Similarity search
Better RAG:
MMR search
Production RAG:
Hybrid search
Advanced RAG:
Multi-query + MMR

⸻
10️⃣ Complete retrieval architecture (real RAG)
Real pipeline:
User Query
    ↓
Query rewriting (optional)
    ↓
Retriever (vector search)
    ↓
Filter results
    ↓
Rank results
    ↓
Context building
    ↓
LLM answer
Retrieval is not just search.
It is:
Search + filtering + ranking
⸻
11️⃣ What you should actually master (important)
If you master these 4 you understand RAG retrieval:
1 similarity search ✔
2 MMR search
3 metadata filtering
4 multi query retrieval
This covers 95% of systems.
⸻
12️⃣ Practical recommendation for you (based on your current stage)
You should next implement in your project:
First:
MMR retrieval.
Then:
Multi-query retrieval.
This gives immediate learning.


2.notes.txt
1️⃣ Reality check: Most beginner RAG looks like this
What tutorials show:
Query
↓
Similarity search
↓
Top chunks
↓
LLM
↓
Answer
This works for demos.
Fails in production.
Why?
Because real documents are messy.
⸻
2️⃣ What real production RAG looks like
Most serious RAG systems use something closer to:
User Query
   ↓
Query optimization
   ↓
Multi-query generation
   ↓
Hybrid retrieval (vector + keyword)
   ↓
MMR re-ranking
   ↓
Metadata filtering
   ↓
Context compression
   ↓
Final context selection
   ↓
LLM answer
Notice:
Retrieval is a pipeline, not one step.
⸻
3️⃣ Real retrieval layers engineers implement
Typical production retrieval stack:
Layer 1 — Query processing
Fix vague queries.
Example:
User:
Year built
System rewrites:
What year was the property built?
Property construction year
This improves recall.
⸻
Layer 2 — Multi-query retrieval
Generate query variations.
Example:
construction year
year built
year constructed
property age
Search all.
Merge results.
⸻
Layer 3 — Hybrid retrieval (very common)
Combine:
Vector search + keyword search.
Because embeddings miss exact terms.
Example:
Policy number:
ABC-123-XYZ
Vector search may miss.
Keyword search finds instantly.
Real companies almost always use hybrid search.
⸻
Layer 4 — MMR ranking
Remove duplicate chunks.
Ensure diversity.
⸻
Layer 5 — Metadata filtering
Example:
If question about:
claims
Filter:
section = claims
Avoid irrelevant data.
⸻
Layer 6 — Context compression
Instead of giving full chunks:
Extract only relevant sentences.
Reduces token cost.
Improves answers.
⸻
4️⃣ What companies actually optimize most
Not embeddings.
Not LLM.
Retrieval quality.
Because:
Bad retrieval = bad answers.
Most RAG failures are retrieval failures.
⸻
5️⃣ What real RAG engineers tune daily
The real knobs engineers adjust:
Chunk size
Chunk overlap
k value
MMR diversity
Query rewriting
Metadata filters
Prompt structure
Not fancy models.
These.
⸻
6️⃣ What real systems often use (actual patterns)
Very common real setups:
Pattern 1 (simple production):
Similarity search
+
Metadata filtering

⸻
Pattern 2 (better production):
Hybrid search
+
MMR
+
Query rewriting

⸻
Pattern 3 (advanced production):
Multi-query
+
Hybrid search
+
Re-ranking model
+
Context compression
This is common in enterprise AI.
⸻
7️⃣ What your current pipeline is missing
Your current pipeline:
Query
↓
Similarity search
↓
LLM
To move toward production quality you would add:
Query
↓
Multi query
↓
MMR retrieval
↓
Context building
↓
LLM
This is the next step.
⸻
8️⃣ Let’s build a realistic production-style retriever (step by step)
Let’s upgrade your retriever with:
1 Multi-query
2 MMR retrieval
3 Clean context building
This matches real engineering practice.
⸻
9️⃣ Example production-style retriever code
Install if needed:
pip install langchain langchain-openai langchain-chroma

⸻
Improved retriever example:
from langchain.retrievers.multi_query import MultiQueryRetriever
from langchain_openai import AzureChatOpenAI
# LLM for query expansion
llm = AzureChatOpenAI(
   openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
   azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
   openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
   deployment_name=os.getenv("AZURE_OPENAI_CHAT_DEPLOYMENT"),
   temperature=0
)
# Base retriever
base_retriever = vectordb.as_retriever(
   search_type="mmr",
   search_kwargs={
       "k":5,
       "fetch_k":20
   }
)
# Multi query retriever
retriever = MultiQueryRetriever.from_llm(
   base_retriever,
   llm
)
results = retriever.invoke(user_query)
for doc in results:
   print(doc.page_content)

⸻
🔟 Why this is closer to real RAG
This now does:
User query
  ↓
LLM generates query variations
  ↓
MMR search ensures diversity
  ↓
Return better context
This is a real production retrieval pattern.
⸻
11️⃣ What separates beginner vs real RAG engineers
Beginner focuses on:
LLM model choice
Real engineers focus on:
Retrieval design
Because:
GPT-4 + bad retrieval = bad system
GPT-3.5 + good retrieval = good system
⸻
12️⃣ The real RAG maturity levels
Level 1 (you are here):
Similarity search RAG
Level 2:
MMR retrieval
Level 3:
Multi-query retrieval
Level 4:
Hybrid search
Level 5:
Re-ranking + compression
You are transitioning from level 1 → 2.
That is correct progression.
⸻
13️⃣ What I recommend you learn next (this matches real engineer progression)
You should next implement:
MMR retrieval first (easy improvement)
Then:
Multi-query retrieval
Then:
Metadata filtering
This is realistic progression.


3.notes3_MMRandMultiplequery.txt
The simplest way to understand the difference
Multi-Query answers:
“What different ways could the user ask this question?”
MMR answers:
“Which retrieved chunks should I keep so they are not repetitive?”
So:
MultiQuery → works on queries
MMR → works on retrieved chunks
Different layers.
⸻
Where they sit in the RAG pipeline
A realistic pipeline:
User Query
↓
MultiQuery (generate variations)
↓
Similarity search
↓
MMR (remove redundant chunks)
↓
Final chunks
↓
LLM answer
So:
MultiQuery happens before retrieval
MMR happens after retrieval
⸻
1️⃣ What MultiQuery does (query expansion)
Example:
User asks:
When was this house constructed?
MultiQuery generates:
construction year
year built
building year
property construction date
So instead of searching once:
Search 1 query
It searches:
Search multiple queries
Goal:
Improve recall (don’t miss relevant chunks)
⸻
2️⃣ What MMR does (result diversification)
After retrieval you might get:
Chunk 1 → Property Overview
Chunk 2 → Property Overview (similar)
Chunk 3 → Property Overview (similar)
Chunk 4 → Construction Notes
Chunk 5 → Claims
MMR changes it to:
Chunk 1 → Property Overview
Chunk 2 → Construction Notes
Chunk 3 → Claims
Chunk 4 → Security
Chunk 5 → Broker
Goal:
Reduce redundancy, increase diversity
⸻
Side-by-side comparison
Feature MultiQuery MMR
Works on Queries Retrieved chunks
Goal Improve recall Improve diversity
Uses LLM Yes No
Happens Before search After search
Problem solved Missed matches Duplicate chunks

⸻
Analogy (simple)
Imagine searching books in a library.
MultiQuery:
Instead of searching:
construction year
You also search:
year built
building date
construction date
More search attempts.
⸻
MMR:
After finding books, you remove:
5 books saying same thing
And keep:
Different useful books.
Less repetition.
⸻
Why we often use both together
Because they solve different problems:
MultiQuery prevents:
Missing relevant chunks
MMR prevents:
Too many similar chunks
Together:
Better retrieval quality.
⸻
Your code actually used both
You wrote:
base_retriever = vectordb.as_retriever(
   search_type="mmr"
)
This enables MMR.
Then:
MultiQueryRetriever.from_llm(...)
This adds MultiQuery.
So your pipeline became:
MultiQuery → MMR → Results
Which is correct.
⸻
Quick mental rule to remember
If problem is:
User phrasing issues → MultiQuery
Too many similar chunks → MMR
Different problems.
⸻
Your real learning status now
You now understand:
✔ Similarity search
✔ MMR retrieval
✔ MultiQuery retrieval
Next missing:
✔ Metadata retrieval
✔ Hybrid retrieval
You are following correct progression.
⸻
Next logical learning step
Now comes something that will actually make your document AI feel much more powerful:
Metadata filtering retrieval
(This is what real document AI systems rely on heavily.)
If ready we continue:
“Start metadata retrieval deep dive”



4.chunkstotextconversationsothatllmunderstands
No.
LLM never sees vectors. LLM only sees normal text chunks.
⸻
What format chunks exist in (two versions)
Every chunk actually exists in two forms in your system:
When you created vector DB:
Chunk text:
"The building was constructed in 1930"
Chunk vector:
[0.21, 0.77, 0.33, 0.91]
So internally database stores:
TEXT  +  VECTOR
But they are used for different purposes.
⸻
Who uses what
Embedding search uses:
Vectors only
LLM uses:
Text only
So flow is:
Text chunk → converted to vector → stored
Query → converted to vector → compared
Matching chunks found
Then:
Vector DB returns TEXT (not vectors)
Important:
Vector DB returns:
chunk.page_content
Which is text.
⸻
Proof from your code
Look here:
context = "\n".join(
   [chunk.page_content for chunk in relevant_chunks]
)
Notice:
chunk.page_content
This is text.
Not vector.
If it was vector it would look like:
[0.21,0.33,0.87]
But it is:
"The building was constructed in 1930"

⸻
What LLM actually receives
Your LLM gets:
QUESTION:
When was building made?
CONTEXT:
The building was constructed in 1930.
Construction completed in 1931.
No numbers.
No vectors.
Only readable text.
⸻
Why LLM cannot read vectors
LLMs are trained on:
Language
Words
Sentences
Documents
Not on:
[0.231,0.445,0.991]
Vectors are mathematical representation only for similarity math.
LLM works on language.
⸻
Simple analogy
Think like this:
Vector DB = Google search
LLM = ChatGPT
Google finds pages using math.
ChatGPT reads pages using language.
ChatGPT does not see Google’s ranking math.
Same here.
⸻
Very clean mental model
Remember this forever:
Vectors are only for FINDING
Text is for UNDERSTANDING
OR even simpler:
Vectors → search
Text → answer

⸻
Full pipeline with formats
Your pipeline actually looks like this:
User query (TEXT)
       ↓
Embedding model
       ↓
Query vector (NUMBERS)
       ↓
Vector similarity search
       ↓
Matching chunk vectors
       ↓
Corresponding chunk TEXT returned
       ↓
LLM reads TEXT
       ↓
Answer generated
Important transition:
VECTOR → TEXT transition happens before LLM

⸻
One line answer
Your question:
chunks that llm sees are they in vector form?
Answer:
No. LLM only sees the chunk text, never the vectors.
Vectors are only used temporarily to find which chunks to send.
⸻
One small extra insight (useful later in your Omnicore project)
When people say:
“LLM reads embeddings”
That is technically wrong.
Correct is:
“Embeddings help select text that LLM reads.”
⸻
If you want, next I can show you something that suddenly makes RAG crystal clear for most engineers:
Why RAG is basically just a smart prompt builder (nothing more).
That realization usually removes the remaining confusion.Good question. This is exactly where many beginners get confused.
Short answer:









Multiqueryflowerexplained
Dummy query
Dummy chunks
Dummy vector DB
So you can visualize like execution.
⸻
Step 0 — Suppose your Vector DB contains these chunks
Imagine your PDF was about a building.
Your vector DB has these chunks:
Chunk1:
The Empire State Building was constructed in 1930.
Chunk2:
Construction started in 1930 and finished in 1931.
Chunk3:
The building is located in New York.
Chunk4:
It has 102 floors.
Chunk5:
The architect was Shreve, Lamb & Harmon.
Chunk6:
The height is 381 meters.
Chunk7:
It was renovated in 2010.
Chunk8:
Construction materials included steel and concrete.
Chunk9:
It was once the tallest building.
Chunk10:
Millions of visitors come every year.

⸻
Step 1 — User asks query
User asks:
"When was the building made?"
This goes here:
retriever.invoke(user_query)

⸻
Step 2 — MultiQueryRetriever creates multiple queries
LLM rewrites query into variations:
Example:
Query1:
When was the building made?
Query2:
When was the building constructed?
Query3:
What is the construction year?
Query4:
When was construction completed?
So now we have:
4 queries instead of 1
Why?
Because maybe document uses:
constructed
completed
built
Instead of:
made
This improves recall.
⸻
Step 3 — Now EACH query searches vector DB
For Query1:
"When was the building made?"
Vector similarity finds:
Chunk1
Chunk2
Chunk7
Chunk3
Chunk8
Chunk5
Chunk9
Chunk4
Chunk6
Chunk10
(fetch_k = 10)
⸻
Step 4 — MMR runs now
MMR selects best 5 diverse chunks.
Instead of picking:
Chunk1
Chunk2
Chunk1
Chunk2
Chunk1
It selects diverse ones:
Example result:
Chunk1 (construction date)
Chunk2 (completion date)
Chunk7 (renovation date)
Chunk3 (location)
Chunk5 (architect)
So Query1 returns:
5 chunks

⸻
Step 5 — Query2 runs same process
Query:
"When was the building constructed?"
Returns maybe:
Chunk1
Chunk2
Chunk8
Chunk7
Chunk9

⸻
Step 6 — Query3 runs
Query:
"What is construction year?"
Returns:
Chunk1
Chunk2
Chunk8
Chunk4
Chunk6

⸻
Step 7 — Query4 runs
Returns:
Chunk2
Chunk1
Chunk7
Chunk3
Chunk10

⸻
Step 8 — Merge all results
Now MultiQuery merges everything:
Before removing duplicates:
Query1:
Chunk1 Chunk2 Chunk7 Chunk3 Chunk5
Query2:
Chunk1 Chunk2 Chunk8 Chunk7 Chunk9
Query3:
Chunk1 Chunk2 Chunk8 Chunk4 Chunk6
Query4:
Chunk2 Chunk1 Chunk7 Chunk3 Chunk10
Total:
20 chunks

⸻
Step 9 — Duplicate removal
Now MultiQuery removes duplicates.
Final unique chunks:
Chunk1
Chunk2
Chunk7
Chunk3
Chunk5
Chunk8
Chunk9
Chunk4
Chunk6
Chunk10
Total:
10 chunks
This becomes:
relevant_chunks

⸻
Step 10 — Context creation
Now your code does:
context = "\n".join(
[chunk.page_content for chunk in relevant_chunks]
)
Context becomes:
The Empire State Building was constructed in 1930.
Construction started in 1930 and finished in 1931.
It was renovated in 2010.
The building is located in New York.
The architect was Shreve, Lamb & Harmon.
Construction materials included steel and concrete.
It was once the tallest building.
It has 102 floors.
The height is 381 meters.
Millions of visitors come every year.

⸻
Step 11 — Final LLM answering
Now LLM gets:
Prompt:
QUESTION:
When was the building made?
CONTEXT:
(all chunks)
LLM reads context.
Finds:
constructed in 1930
finished in 1931
So answer becomes:
The building was constructed in 1930 and completed in 1931.

⸻
Visual Flow (SUPER SIMPLE)
This is your code in simplest visual:
User Query
"When was building made?"
       ↓
MultiQuery creates:
constructed?
built?
completion date?
       ↓
Each query searches DB
       ↓
MMR selects 5 per query
       ↓
All results merged
       ↓
Duplicates removed
       ↓
Final chunks list
       ↓
LLM reads chunks
       ↓
Answer generated

⸻
Your misunderstanding (main confusion)
You were thinking:
MultiQuery → LLM → MMR → chunks
But real flow:
MultiQuery creates queries
       ↓
Each query uses MMR search
       ↓
MultiQuery merges results
       ↓
Duplicates removed

⸻
One sentence summary (remember this)
Your pipeline is:
MultiQuery = better questions
MMR = better chunk selection
LLM = better answer

⸻
If you want I can next show what would happen if you REMOVE MultiQuery (so you see why it is powerful).
That usually makes the concept click instantly.Good. Let’s simulate your exact code with dummy data so you can see what happens step-by-step.
We will act like the computer and manually run your pipeline.
I’ll use:
Dummy query
Dummy chunks
Dummy vector DB
So you can visualize like execution.



6.Metadatafiltering
First we must understand one fundamental thing:
Step 1 — What metadata actually is in LangChain
When you load a document, LangChain does NOT just store text.
It stores a Document object.
A Document looks like this internally:
Document(
   page_content="Year Built 1998...",
   metadata={"source":"mytext.txt"}
)
So every chunk has:
1 page_content → actual text
2 metadata → information about the text
This is critical.
⸻
Step 2 — What metadata is used for
Metadata is used to answer:
Where did this chunk come from?
Which section?
Which page?
Which document?
Which type?
Example:
Instead of just:
Year Built 1998
We store:
Document(
   page_content="Year Built 1998",
   metadata={
       "section":"property",
       "document":"submission",
       "page":2
   }
)
Now retrieval can filter:
Search only property section.
This is extremely powerful.
⸻
Step 3 — Why metadata is powerful
Right now your retrieval does:
Search whole document.
With metadata you can do:
Search only:
Property section
Claims section
Construction section
Example:
User asks:
Year Built
Instead of searching everything:
System does:
Filter section = property
Then search.
This improves:
✔ Speed
✔ Precision
✔ Accuracy
✔ Cost
This is why production document AI relies heavily on metadata.
⸻
Step 4 — Where metadata comes from
Metadata is usually added during:
Chunking stage
Right now your ingestion:
chunks = text_splitter.split_documents(documents)
Already creates metadata automatically like:
metadata={
  "source":"mytext.txt"
}
But we can add more.
⸻
Step 5 — Let’s inspect your current metadata
Add this in your ingestion script after chunking:
for chunk in chunks:
   print(chunk.metadata)
You will likely see:
{'source': 'C:\\Users\\...\\mytext.txt'}
This proves metadata already exists.
⸻
Step 6 — Adding custom metadata (simple example)
We can manually add metadata like this:
Example:
for chunk in chunks:
   chunk.metadata["document_type"]="insurance_submission"
   chunk.metadata["category"]="underwriting"
Now metadata becomes:
{
'source':'mytext.txt',
'document_type':'insurance_submission',
'category':'underwriting'
}
Very simple.
⸻
Step 7 — Why this matters later
Because later we can retrieve like:
vectordb.similarity_search(
   query,
   filter={"document_type":"insurance_submission"}
)
Meaning:
Search only this type.
This is real enterprise retrieval.
⸻
Step 8 — First practical step we will implement
We will now add:
Simple metadata tagging
Example:
All chunks belong to:
Document type = insurance
Then we test filtering.
This is first step.
⸻
Step 9 — Modify your ingestion code
In 4.embeddings_my_querytopdf.py
After:
chunks = text_splitter.split_documents(documents)
Add:
for chunk in chunks:
   chunk.metadata["doc_type"]="insurance"
   chunk.metadata["source_file"]="mytext.txt"
That’s it.
Now chunks contain metadata.
⸻
Step 10 — Rebuild vector DB (important)
Because metadata is stored with vectors.
Delete:
input folder
Then rerun ingestion:
python 4.embeddings_my_querytopdf.py
Always rebuild when metadata changes.
⸻
Step 11 — Verify metadata stored
Add temporary debug:
for chunk in chunks:
   print(chunk.metadata)
You should see:
{'source':'mytext.txt','doc_type':'insurance','source_file':'mytext.txt'}
Good.
⸻
Step 12 — Next step (what we will do next)
Now we have metadata stored.
Next lesson we will learn:
How to filter retrieval using metadata.
Example:
vectordb.similarity_search(
   query,
   filter={"doc_type":"insurance"}
)
This is where metadata becomes powerful.
⸻
Important note
We are going slowly because this concept becomes foundation for:
Agents
Extraction pipelines
Document routing
Memory systems
Tool selection
Metadata is everywhere in agent systems.
⸻
Where you are now
You now understand:
✔ Document has page_content
✔ Document has metadata
✔ Metadata travels with chunks
✔ Metadata stored in vector DB
Next we use it.
⸻
Next step (we continue building)
Next we implement:
Metadata filtering retrieval (actual usage)
We will modify your retriever to filter using metadata.
































------------------------------------------------------------------------------------------------------------------------------------------------------

4.embeddings_my_querytopdf.py (FULL CODE – ingestion)
Copy completely:
from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings
from langchain_community.document_loaders import TextLoader
from langchain_text_splitters import RecursiveCharacterTextSplitter
from dotenv import load_dotenv
import os
load_dotenv()
# Embedding model
embeddings = AzureOpenAIEmbeddings(
   azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),
   openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
   openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
   azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
   chunk_size=1024
)
# Text splitter
text_splitter = RecursiveCharacterTextSplitter(
   chunk_size=800,
   chunk_overlap=100
)
# Load document
loader = TextLoader(
   r"C:\Users\2000137378\Desktop\newproject\RAG_PIPELINES\mytext.txt",
   encoding="utf-8"
)
documents = loader.load()
# Split into chunks
chunks = text_splitter.split_documents(documents)
# ADD METADATA HERE
for chunk in chunks:
   chunk.metadata["doc_type"] = "insurance"
   chunk.metadata["category"] = "underwriting"
   chunk.metadata["source_file"] = "mytext.txt"

# Directory to store vectors
my_dir = os.path.join(
   os.path.dirname(os.path.abspath(__file__)),
   "input"
)
# Create vector DB
vectorstore = Chroma.from_documents(
   documents=chunks,
   embedding=embeddings,
   collection_name="my_collection",
   persist_directory=my_dir
)
print("\nTotal chunks:",len(chunks))
print("\nFirst chunk text:")
print(chunks[0].page_content[:150])
print("\nFirst chunk metadata:")
print(chunks[0].metadata)

⸻
IMPORTANT STEP (DO THIS)
Delete your vector DB first:
Delete folder:
RAG_PIPELINES/input
Then run:
python 4.embeddings_my_querytopdf.py
You should see metadata printed.
⸻
6.retriever.py (FULL CODE – metadata filtering retrieval)
Copy fully:
from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings, AzureChatOpenAI
from langchain_core.prompts import PromptTemplate
from langchain_classic.chains.llm import LLMChain
from dotenv import load_dotenv
import os
import asyncio
load_dotenv()
# Embeddings
embeddings = AzureOpenAIEmbeddings(
   azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),
   openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
   openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
   azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
   chunk_size=1024
)

def query_documents(vectordb, user_query):
   print("\nProcessing query:",user_query)
   llm = AzureChatOpenAI(
       openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
       azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
       openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
       deployment_name=os.getenv("AZURE_OPENAI_CHAT_DEPLOYMENT"),
       temperature=0
   )
   print("\nPerforming metadata filtered search...")
   # METADATA FILTER HERE
   relevant_chunks = vectordb.similarity_search(
       user_query,
       k=5,
       filter={"doc_type":"insurance"}
   )
   print("\nChunks retrieved:",len(relevant_chunks))
   if not relevant_chunks:
       return "No relevant information found"
   # Print metadata to confirm filtering
   print("\nMetadata of retrieved chunks:")
   for chunk in relevant_chunks:
       print(chunk.metadata)
   # Build context
   context = "\n".join(
       chunk.page_content for chunk in relevant_chunks
   )
   # Prompt
   qa_prompt = PromptTemplate(
       input_variables=["context","question"],
       template="""
Answer the question using only the context.
QUESTION:
{question}
CONTEXT:
{context}
ANSWER:
"""
   )
   chain = LLMChain(
       llm=llm,
       prompt=qa_prompt
   )
   answer = chain.run({
       "context":context,
       "question":user_query
   })
   return answer

async def process_query():
   vector_db_path = r"C:\Users\2000137378\Desktop\newproject\RAG_PIPELINES\input"
   vectordb = Chroma(
       embedding_function=embeddings,
       persist_directory=vector_db_path,
       collection_name="my_collection"
   )
   answer = query_documents(
       vectordb,
       "Give me the Year Built"
   )
   print("\nFinal Answer:\n")
   print(answer)

if __name__ == "__main__":
   asyncio.run(process_query())

⸻
Expected output
You should see:
Processing query: Give me the Year Built
Performing metadata filtered search...
Chunks retrieved: 5
Metadata:
{'doc_type':'insurance',...}
Final Answer:
Year Built: 1998 (renovations 2019, 2023)

⸻
What you just built (important)
You now have:
Document
↓
Chunking
↓
Metadata tagging
↓
Vector storage
↓
Metadata filtered retrieval
↓
LLM answer
This is real production RAG structure.
⸻
Next real upgrade (what engineers do next)
Next powerful step would be:
Automatic section detection
Example:
Chunk contains:
Claims History
Metadata becomes:
section = claims
Then retrieval becomes:
filter={"section":"claims"}
This is how document AI becomes structured.
⸻
If you want next real engineer step:
“Next automatic section metadata tagging”
(This is where your underwriting AI starts becoming truly intelligent.)







7.MultiQueryRetrieval
from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings, AzureChatOpenAI
# from langchain.retrievers.multi_query import MultiQueryRetriever
from langchain_classic.retrievers import MultiQueryRetriever
from dotenv import load_dotenv
import os







from langchain_chroma import Chroma
from langchain_openai import AzureOpenAIEmbeddings
import logging
from dotenv import load_dotenv
import os


from typing import Dict, Any, List
# from langchain.chains import LLMChain
from langchain_classic.chains.llm import LLMChain
# from langchain.prompts import PromptTemplate
from langchain_core.prompts import PromptTemplate
# from langchain_community.chat_models import AzureChatOpenAI
from langchain_openai import AzureChatOpenAI, AzureOpenAIEmbeddings
# from langchain_community.vectorstores import Chroma
from langchain_chroma import Chroma
# from langchain_community.embeddings import AzureOpenAIEmbeddings
# from azure.storage.blob import BlobServiceClient, ContainerClient
from azure.core.exceptions import ResourceExistsError
import re
# from datetime import datetime
# from urllib.parse import urlparse, unquote
# from Jarvis.MCP.common.phoenix_prompt import build_prompt

import asyncio



load_dotenv()
# # embeddings (same as ingestion)
# embeddings = AzureOpenAIEmbeddings(
#    azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),
#    openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
#    openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
#    azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT")
# )
# # load YOUR collection

# my_dir = os.path.abspath(os.path.join(os.path.dirname(__file__), '..', 'input'))


# vectordb = Chroma(
#    embedding_function=embeddings,
#    persist_directory=my_dir,
#    collection_name="my_collection"
# )
# # LLM for query expansion
# llm = AzureChatOpenAI(
#    openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
#    azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
#    openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
#    deployment_name=os.getenv("AZURE_OPENAI_CHAT_DEPLOYMENT"),
#    temperature=0
# )
# # base retriever (MMR)
# base_retriever = vectordb.as_retriever(
#    search_type="mmr",
#    search_kwargs={
#        "k":5,
#        "fetch_k":20
#    }
# )
# # multi query retriever
# retriever = MultiQueryRetriever.from_llm(
#    base_retriever,
#    llm
# )
# # query
# user_query = "Give me the Year Built"
# results = retriever.invoke(user_query)
# print("\nRetrieved chunks:\n")
# for doc in results:
#    print(doc.page_content[:200])
#    print("------")






logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# Initialize Azure OpenAI embeddings
embeddings = AzureOpenAIEmbeddings(
    azure_deployment=os.getenv("AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME"),
    openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),
    openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),
    azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),
    chunk_size=1024
)



def query_documents(vectordb: Any, user_query: str) -> str:

    print(f"🔍 Processing query: {user_query}")

    llm = AzureChatOpenAI(

        openai_api_key=os.getenv("AZURE_OPENAI_API_KEY"),

        azure_endpoint=os.getenv("AZURE_OPENAI_ENDPOINT"),

        openai_api_version=os.getenv("AZURE_OPENAI_API_VERSION"),

        deployment_name=os.getenv("AZURE_OPENAI_CHAT_DEPLOYMENT"),

        temperature=0,

        top_p=1

    )

    print("📚 Performing MultiQuery retrieval...")

    base_retriever = vectordb.as_retriever(

        search_type="mmr",

        search_kwargs={

            "k":5,

            "fetch_k":10

        }

    )

    retriever = MultiQueryRetriever.from_llm(

        base_retriever,

        llm

    )

    relevant_chunks = retriever.invoke(user_query)

    print("Retrieved chunks:",len(relevant_chunks))

    if not relevant_chunks:

        return "No relevant information found."

    # Debug chunks

    for i,chunk in enumerate(relevant_chunks):

        print(f"\nChunk {i}:")

        print(chunk.page_content[:150])

    context = "\n".join(

        [chunk.page_content for chunk in relevant_chunks]

    )

    qa_prompt = PromptTemplate(

        input_variables=["context","question"],

        template="""You are an expert assistant for answering questions about documents.

Based on the provided document context, answer the user's question accurately and concisely.

QUESTION: {question}

CONTEXT:
{context}

ANSWER:
Provide a clear, direct answer based only on the context provided. 
If the answer is not found in the context, explicitly state "Not found in documents."
Do not guess or use external knowledge.
Keep your answer concise and relevant to the question."""

    )

    chain = LLMChain(

        llm=llm,

        prompt=qa_prompt

    )

    answer = chain.run({

        "context":context,

        "question":user_query

    }).strip()

    return answer
 



 
async def process_query_retrieval(vector_db_path: str, query: str) -> Dict[str, Any]:
    """
    Main processing function for query-based document retrieval.
    
    Takes a user query, performs semantic search on vectorized content,
    and returns a direct answer without JSON extraction.
    
    Args:
        vector_db_path: Can be either:
            1. Local absolute path from pdf_vectorizer output: /home/jarvis/rohan/Document_Query_MCP/data/vec_db/pdf_name
            2. Blob URL (fallback): https://... - will extract pdf name and construct local path
        query: User's question as a string
    
    Returns:
        Dict with answer and metadata
    """
    try:
        # Handle both blob URLs and local paths
        # if vector_db_path.startswith(("http://", "https://")):
            # This is a blob URL - extract the PDF name and construct local path
        #     logger.warning(f"Received blob URL instead of local path. Extracting PDF name...")
        #     path = unquote(urlparse(vector_db_path).path)
        #     file_name_with_ext = os.path.basename(path)
        #     pdf_name = os.path.splitext(file_name_with_ext)[0]
            
        #     # Construct the local vector_db_path
        #     vdb_base_path = os.getenv("QUERY_VDB_PATH", "./data/vec_db")
        #     vector_db_path = os.path.abspath(os.path.join(vdb_base_path, pdf_name))
        #     logger.info(f"Reconstructed local vector_db_path: {vector_db_path}")
        # else:
            # Ensure it's an absolute path
        vector_db_path = os.path.abspath(vector_db_path)
        
        # Get safe collection name from vector_db_path
        # ChromaDB only allows: [a-zA-Z0-9._-]
        # safe_name = os.path.basename(vector_db_path).lower()
        # safe_name = safe_name.replace(" ", "_").replace("-", "_").replace(".", "_")
        # # Remove parentheses and other special characters
        # safe_name = "".join(c if c.isalnum() or c in "._-" else "" for c in safe_name)
        # # Ensure it starts and ends with alphanumeric
        # safe_name = safe_name.strip("._-")
        # collection_name = f"{safe_name}_collection"
        # persist_directory = os.path.join(vector_db_path, "chroma_db", safe_name)
        
        # Verify the vector DB exists
        if not os.path.exists(vector_db_path):
            raise FileNotFoundError(f"Vector database not found at: {vector_db_path}")
        
        # Load the vector store
        print(f"📚 Loading vector store from {vector_db_path}")
        vectordb = Chroma(
            embedding_function=embeddings,
            persist_directory=vector_db_path,
            collection_name="my_collection"
        )
        
        # Get answer using semantic search and LLM
        answer = query_documents(vectordb, query)
        # Get PDF name for metadata
        pdf_name = os.path.basename(vector_db_path)
        
        return {
            "answer": answer,
            "metadata": {
                "is_error": False,
                "error_message": None,
                "query": query,
                "source_pdf": pdf_name,
                "vector_db_path": vector_db_path
            }
        }
        
    except Exception as e:
        logger.exception("Error in query retrieval process")
        return {
            "answer": None,
            "metadata": {
                "is_error": True,
                "error_message": str(e),
                "query": query,
                "source_pdf": None,
                "vector_db_path": vector_db_path if 'vector_db_path' in locals() else None
            }
        }





# if __name__ == "__main__":
#     await process_query_retrieval("C:\\Users\\2000137378\\Desktop\\newproject\\RAG_PIPELINES\\input","What is Contact Number")
    
if __name__ == "__main__":
    result = asyncio.run(process_query_retrieval(
        r"C:\Users\2000137378\Desktop\newproject\RAG_PIPELINES\input",
        "When was this building made?"
    ))
    print(result)    







metadafiletring
Step 1 — The real problem we solve
Your document contains sections like:
Policyholder Details
Property Overview
Claims History
Construction Notes
Security Features
But currently chunks only have:
metadata={"doc_type":"insurance"}
We want:
metadata={
"doc_type":"insurance",
"section":"property"
}
So retrieval becomes:
filter={"section":"property"}
This is powerful.
⸻
Step 2 — Simple engineer approach (how companies actually start)
Engineers don’t start with ML.
They start simple:
Rule based section detection
Example rule:
If chunk contains "Property Overview" → section=property
If contains "Claims History" → section=claims
If contains "Security Features" → section=security
Simple and effective.
We will do that.
⸻
Step 3 — Modify ingestion code
Modify your ingestion script.
Replace your metadata block with this:
# Add metadata automatically based on section
for chunk in chunks:
   text = chunk.page_content.lower()
   chunk.metadata["doc_type"] = "insurance"
   chunk.metadata["source_file"] = "mytext.txt"

   # SECTION DETECTION RULES
   if "property overview" in text:
       chunk.metadata["section"] = "property"

   elif "claims history" in text:
       chunk.metadata["section"] = "claims"

   elif "construction" in text:
       chunk.metadata["section"] = "construction"

   elif "security features" in text:
       chunk.metadata["section"] = "security"

   elif "policyholder" in text:
       chunk.metadata["section"] = "policyholder"

   else:
       chunk.metadata["section"] = "general"
This is how most production systems begin.
⸻
Step 4 — Verify tagging works
Add debug:
print("\nMetadata check:\n")
for chunk in chunks:
   print(chunk.metadata)
Expected:
{'doc_type':'insurance','section':'property'}
{'doc_type':'insurance','section':'claims'}
{'doc_type':'insurance','section':'construction'}

⸻
Step 5 — Rebuild vector DB
Again:
Delete:
input folder
Then run:
python 4.embeddings_my_querytopdf.py
Always rebuild after metadata change.
⸻
Step 6 — Use section filtering in retrieval
Now modify your retrieval:
Instead of:
filter={"doc_type":"insurance"}
Use:
filter={
   "doc_type":"insurance",
   "section":"property"
}
Example:
relevant_chunks = vectordb.similarity_search(
   user_query,
   k=5,
   filter={
       "doc_type":"insurance",
       "section":"property"
   }
)
Now you search only property section.
⸻
Step 7 — Test it
Change query:
user_query="Give me the Year Built"
Property section contains Year Built.
Should still work.
Now try:
filter={"section":"claims"}
Then query:
"What was the claim amount?"
You will see different chunks retrieved.
This proves filtering works.
⸻
Step 8 — What real systems improve next
Real systems then make section detection smarter:
Instead of:
simple keyword rules
They move to:
Regex detection
Heading parsing
PDF structure extraction
LLM classification
But rules first.
Always.
⸻
Step 9 — What you just learned (important milestone)
You now understand:
✔ Metadata tagging
✔ Section detection
✔ Structured retrieval
✔ Section filtering
This is already enterprise RAG foundation.
Most tutorials never reach this.
⸻
Step 10 — What real engineers build next
Now RAG starts getting interesting.
Next real upgrades engineers add:
Next possible steps:
1️⃣ Dynamic metadata filtering (detect section from query)
Example:
Query:
Claim amount
System automatically chooses:
filter={"section":"claims"}
This is very realistic.
⸻
2️⃣ Hybrid retrieval (vector + keyword)
Very important for:
Policy numbers
IDs
Addresses
⸻
3️⃣ Context compression
Reduce tokens before LLM.
⸻
Correct next step for YOU
Since you are building underwriting AI, the most logical next step is:
Dynamic section selection from query
Meaning:
User asks:
Year built
System automatically decides:
section="property"
No manual filter.
This is how real document agents work.

metadatafiltering
Good — let’s slow down and understand exactly how your current metadata filtering code works, step-by-step, both conceptually and code flow wise. This is important before moving further.
I’ll explain using your exact code flow.
⸻
First understand what your system currently does
Your current pipeline is:
Load document
↓
Split into chunks
↓
Add metadata to chunks
↓
Store chunks in Chroma
↓
User query comes
↓
Filter chunks using metadata
↓
Similarity search on filtered chunks
↓
Send to LLM
↓
Answer
Now let’s trace it like execution.
⸻
PART 1 — Ingestion working (8.1metadatafilteringloader.py)
Step 1 — Load document
Your code:
loader = TextLoader("mytext.txt")
documents = loader.load()
What you get:
[
Document(
 page_content="Home Insurance Underwriting...",
 metadata={"source":"mytext.txt"}
)
]
Important:
documents = list of Document objects
⸻
Step 2 — Split into chunks
Your code:
chunks = text_splitter.split_documents(documents)
Now this becomes:
[
Document(page_content="Chunk 1",metadata={}),
Document(page_content="Chunk 2",metadata={}),
Document(page_content="Chunk 3",metadata={})
]
Each chunk is still a Document object.
Not just text.
⸻
Step 3 — Add metadata
Your code:
for chunk in chunks:
  chunk.metadata["doc_type"] = "insurance"
  chunk.metadata["category"] = "underwriting"
  chunk.metadata["source_file"] = "mytext.txt"
Now each chunk becomes:
Document(
page_content="Year Built 1998",
metadata={
  "source":"mytext.txt",
  "doc_type":"insurance",
  "category":"underwriting",
  "source_file":"mytext.txt"
}
)
Important:
You are modifying the Document object, not copying it.
This metadata travels with the chunk.
⸻
Step 4 — Store in Chroma
Your code:
vectorstore = Chroma.from_documents(
  documents=chunks,
  embedding=embeddings,
  collection_name="mymynew_collection",
  persist_directory=my_dir
)
What happens internally:
For each chunk:
Step 4.1 Generate embedding:
"Year Built 1998" → [0.23,0.91,0.44...]
Step 4.2 Store:
Chroma stores:
Vector
Text
Metadata
ID
Example:
Vector:
[0.23,0.91,0.44...]
Text:
Year Built 1998...
Metadata:
{
"doc_type":"insurance",
"category":"underwriting"
}
Important:
Metadata is stored along with vector.
This is key.
⸻
PART 2 — Retrieval working (8.2metadatafilteringretrieval.py)
Now query flow.
⸻
Step 5 — Load vector DB
Your code:
vectordb = Chroma(
   embedding_function=embeddings,
   persist_directory=vector_db_path,
   collection_name="mymynew_collection"
)
This loads:
All vectors
All metadata
All text
Nothing searched yet.
Just loading database.
⸻
Step 6 — Query comes
Your code:
user_query="Give me the Year Built"

⸻
Step 7 — Query embedding created
Your code:
relevant_chunks = vectordb.similarity_search(
   user_query,
   k=5,
   filter={"doc_type":"insurance"}
)
Internally:
Step 7.1 Query converted to vector:
"Give me the Year Built"
↓
[0.51,0.22,0.88...]

⸻
Step 8 — Metadata filtering happens FIRST
This is critical:
Chroma does:
Filter metadata first
THEN similarity search
So first:
Find chunks where:
doc_type="insurance"
Which is all your chunks.
So filtered pool:
7 chunks

⸻
Step 9 — Similarity search happens SECOND
Now similarity search happens only on filtered chunks.
Chroma calculates:
Cosine similarity
Between:
Query vector:
[0.51,0.22,0.88]
And each chunk vector:
Chunk1 → similarity 0.82
Chunk2 → similarity 0.44
Chunk3 → similarity 0.91
Chunk4 → similarity 0.62
Then picks:
Top k=5

⸻
Step 10 — Returned results
You get:
[
Document(chunk1),
Document(chunk3),
Document(chunk4),
Document(chunk5),
Document(chunk7)
]
These are Document objects again.
⸻
Step 11 — Build context
Your code:
context = "\n".join(
  chunk.page_content for chunk in relevant_chunks
)
Meaning:
Take text only:
Year Built 1998
Construction Timber
Claims amount $2480
Metadata not sent to LLM.
Only text.
Important design decision.
⸻
Step 12 — Send to LLM
Your code:
chain.run({
"context":context,
"question":user_query
})
Prompt becomes:
QUESTION:
Give me the Year Built
CONTEXT:
Year Built 1998...
ANSWER:

⸻
Step 13 — LLM answers
LLM reads context:
Finds:
Year Built 1998
Responds:
Year Built: 1998

⸻
Full flow in one diagram
Your exact system:
Text file
↓
Document objects
↓
Chunking
↓
Add metadata
↓
Embedding creation
↓
Store vector + metadata
↓
User query
↓
Query embedding
↓
Metadata filter
↓
Similarity search
↓
Top chunks
↓
Context building
↓
LLM answer
This is basic production RAG design.
⸻
Key concept most important to remember
Metadata filtering is:
Pre-filter
Similarity search is:
Ranking
So order:
Filter → Rank → Return
Not:
Rank → Filter
This is critical.
⸻
Very important understanding check
Your system currently does:
Search insurance chunks
Then find most similar
Later systems do:
Find correct section
Then search
Then rerank
Then compress
You are building foundation.
⸻
What you now truly understand
You now know:
✔ Document objects
✔ Metadata storage
✔ Vector + metadata storage
✔ Metadata filtering order
✔ Similarity ranking
✔ Context building
✔ LLM answering
This is real RAG core.
⸻
Before next topic I want to strengthen your foundation:
Next important concept you should understand:
What exactly happens inside similarity_search()
Meaning:
How vectors are compared mathematically.
This will connect your ML understanding with RAG.
If you want we go there next:
“Explain similarity_search internally”Good — let’s slow down and understand exactly how your current metadata filtering code works, step-by-step, both conceptually and code flow wise. This is important before moving further.
































-----------------------------------------------------------------------------------------------------------------------------------------------------------------Excellent observation. You just noticed something very important:

Your metadata filter currently does nothing useful.

You are absolutely correct.

Let’s analyze honestly.

⸻

What your current filter is doing

You stored:

chunk.metadata["doc_type"]="insurance"

for every chunk.

So your DB looks like:

Chunk	doc_type

Chunk1	insurance

Chunk2	insurance

Chunk3	insurance

Chunk4	insurance

Chunk5	insurance

Chunk6	insurance

Chunk7	insurance

Now your filter:

filter={"doc_type":"insurance"}

Means:

Give me all chunks where doc_type = insurance

Result:

All chunks match.

So yes — filter is useless right now.

You are still searching entire document.

You understood correctly.

⸻

When metadata filtering actually becomes useful

Metadata filtering only helps when chunks have different values.

Example real system:

Chunk	doc_type	section

Chunk1	insurance	property

Chunk2	insurance	property

Chunk3	insurance	claims

Chunk4	insurance	security

Chunk5	insurance	construction

Chunk6	insurance	claims

Chunk7	insurance	general

Now filter:

filter={"section":"property"}

Result:

Returned

Chunk1

Chunk2

Now search happens only on these.

This is when metadata becomes powerful.

⸻

Why we added doc_type first

We started simple:

doc_type="insurance"

Because real systems usually store multiple document types:

Example:

Chunk	doc_type

Chunk1	insurance_submission

Chunk2	inspection_report

Chunk3	quote_document

Chunk4	acord_form

Then filter:

filter={"doc_type":"quote_document"}

Now search happens only in that document type.

That is real usage.

⸻

Your current document situation

You only have:

One document

One doc_type

So metadata filter gives no benefit yet.

This is expected.

⸻

When your system becomes realistic

Metadata filtering becomes powerful when you have:

Multiple documents

Example:

Submission report

Inspection report

Quote document

ACORD form

Loss report

Then:

filter={"doc_type":"submission"}

Matters.

⸻

Or multiple sections

Example:

Property section

Claims section

Security section

Then:

filter={"section":"claims"}

Matters.

⸻

Important realization you just had (this is engineer thinking)

You just asked:

“If all chunks have same metadata, what is the point?”

This is exactly how RAG engineers think.

They don’t blindly add metadata.

They ask:

Does this metadata actually reduce search space?

If no → useless.

⸻

So right now your system effectively does:

Filter insurance (all chunks)

↓

Similarity search

↓

Answer

Which is same as:

Similarity search only

Correct.

⸻

When metadata becomes meaningful for YOU

For your underwriting document, useful metadata would be:

Example:

chunk.metadata["section"]="property"

chunk.metadata["section"]="claims"

chunk.metadata["section"]="construction"

chunk.metadata["section"]="security"

Then filtering becomes meaningful.

⸻

What real engineers would do next (logical step)

Since you only have one document, the next useful step is:

Section-based metadata

Because your document clearly has:

Property Overview

Claims History

Construction Notes

Security Features

This is natural metadata.

Not artificial like doc_type.

⸻

Real answer to your question

You asked:

Why metadata if it still considers all chunks?

Answer:

Right now:

Metadata is just demonstration.

Later:

Metadata becomes search control.


⸻

Your current learning stage is correct

You are learning:

Step 1:

How metadata works

Next:

How metadata becomes useful

Which is:

Section tagging (this is where benefit starts).

⸻

Honest next logical step

Now we should modify ingestion to store:

chunk.metadata["section"]="property"

chunk.metadata["section"]="claims"

Then test filtering.

That will make the difference visible.

⸻

Next step (this is where it becomes interesting)

We should now build:

Section-aware ingestion (proper metadata design)

This is where metadata actually starts helping retrieval.

Say:

“Build proper section metadata system”

This will make metadata filtering feel useful instead of theoretical.Excellent observation. You just noticed something very important:

Your metadata filter currently does nothing useful.

You are absolutely correct.

Let’s analyze honestly.

⸻

What your current filter is doing

You stored:

chunk.metadata["doc_type"]="insurance"

for every chunk.

So your DB looks like:

Chunk	doc_type

Chunk1	insurance

Chunk2	insurance

Chunk3	insurance

Chunk4	insurance

Chunk5	insurance

Chunk6	insurance

Chunk7	insurance

Now your filter:

filter={"doc_type":"insurance"}

Means:

Give me all chunks where doc_type = insurance

Result:

All chunks match.

So yes — filter is useless right now.

You are still searching entire document.

You understood correctly.

⸻

When metadata filtering actually becomes useful

Metadata filtering only helps when chunks have different values.

Example real system:

Chunk	doc_type	section

Chunk1	insurance	property

Chunk2	insurance	property

Chunk3	insurance	claims

Chunk4	insurance	security

Chunk5	insurance	construction

Chunk6	insurance	claims

Chunk7	insurance	general

Now filter:

filter={"section":"property"}

Result:

Returned

Chunk1

Chunk2

Now search happens only on these.

This is when metadata becomes powerful.

⸻

Why we added doc_type first

We started simple:

doc_type="insurance"

Because real systems usually store multiple document types:

Example:

Chunk	doc_type

Chunk1	insurance_submission

Chunk2	inspection_report

Chunk3	quote_document

Chunk4	acord_form

Then filter:

filter={"doc_type":"quote_document"}

Now search happens only in that document type.

That is real usage.

⸻

Your current document situation

You only have:

One document

One doc_type

So metadata filter gives no benefit yet.

This is expected.

⸻

When your system becomes realistic

Metadata filtering becomes powerful when you have:

Multiple documents

Example:

Submission report

Inspection report

Quote document

ACORD form

Loss report

Then:

filter={"doc_type":"submission"}

Matters.

⸻

Or multiple sections

Example:

Property section

Claims section

Security section

Then:

filter={"section":"claims"}

Matters.

⸻

Important realization you just had (this is engineer thinking)

You just asked:

“If all chunks have same metadata, what is the point?”

This is exactly how RAG engineers think.

They don’t blindly add metadata.

They ask:

Does this metadata actually reduce search space?

If no → useless.

⸻

So right now your system effectively does:

Filter insurance (all chunks)

↓

Similarity search

↓

Answer

Which is same as:

Similarity search only

Correct.

⸻

When metadata becomes meaningful for YOU

For your underwriting document, useful metadata would be:

Example:

chunk.metadata["section"]="property"

chunk.metadata["section"]="claims"

chunk.metadata["section"]="construction"

chunk.metadata["section"]="security"

Then filtering becomes meaningful.

⸻

What real engineers would do next (logical step)

Since you only have one document, the next useful step is:

Section-based metadata

Because your document clearly has:

Property Overview

Claims History

Construction Notes

Security Features

This is natural metadata.

Not artificial like doc_type.

⸻

Real answer to your question

You asked:

Why metadata if it still considers all chunks?

Answer:

Right now:

Metadata is just demonstration.

Later:

Metadata becomes search control.


⸻

Your current learning stage is correct

You are learning:

Step 1:

How metadata works

Next:

How metadata becomes useful

Which is:

Section tagging (this is where benefit starts).

⸻

Honest next logical step

Now we should modify ingestion to store:

chunk.metadata["section"]="property"

chunk.metadata["section"]="claims"

Then test filtering.

That will make the difference visible.

⸻

Next step (this is where it becomes interesting)

We should now build:

Section-aware ingestion (proper metadata design)

This is where metadata actually starts helping retrieval.

Say:

“Build proper section metadata system”

This will make metadata filtering feel useful instead of theoretical.
 



















