
Now let’s check main.py include_operations for these two, and check the vendor init_db.py schema for vendor_master_input and vendor_jobs_input.

I went through all 10 Vendor Manager agent handlers directly (not just the README, since — like SIU — it turned out to be stale in two places). Here’s both pieces.

1. Complexity ranking (simplest → most complex)

Tier 1 — Simple (pure SQL/arithmetic, no LLM, single-purpose)



|Agent                       |Why it’s simple                                                                                                                                                    |
|----------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------|
|**SLAComplianceAgent**      |3 functions. `compute_sla_compliance` is just `% of vendor_jobs_input rows where sla_status != 'Overdue'` + an upsert. No LLM, no branching logic worth mentioning.|
|**VendorPerformanceAgent**  |3 functions. `compute_vendor_performance_score` = average of 3 simple ratios (completion rate, avg rating scaled, 100−                                             |
|**EscalationAgent**         |3 functions. `escalate_overdue_jobs` just scans `vendor_jobs_input` for `sla_status='Overdue'` and inserts a log row per hit. No LLM, no scoring.                  |
|**VendorCostBenchmarkAgent**|4 functions. `compute_cost_variance` is a straight average/variance calculation over `vendor_cost_input` rows. No LLM.                                             |

Tier 2 — Moderate (more functions/state, still no LLM)



|Agent                    |Why it’s moderate                                                                                                                                                                                      |
|-------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
|**VendorMatchingAgent**  |6 functions incl. a heuristic loss_type→specialty map and a ranked top-3 vendor query, plus an assignment upsert. No LLM but more moving parts.                                                        |
|**VendorOnboardingAgent**|6 functions; `approve_vendor_application` is a multi-table provisioning step (application → `vendors` row → `vendor_master_input` row with derived defaults). No LLM, but touches 3 tables in one call.|
|**DispatchAgent**        |5 functions; `work_orders` has ~25 columns and a status state machine (Scheduled/In Progress/Completed/Canceled) with timestamp side-effects. No LLM, but the widest single table in the persona.      |

Tier 3 — Most complex (LLM-in-the-loop, multi-signal reasoning)



|Agent                            |Why it’s complex                                                                                                                      |
|---------------------------------|--------------------------------------------------------------------------------------------------------------------------------------|
|**ETAPredictionAgent**           |LLM call (with heuristic fallback) reasoning over baseline ETA + loss_type/complexity to produce a prediction with confidence/factors.|
|**VendorQualificationAgent**     |LLM call over 4 compliance checks to decide eligibility/risk. **Also currently broken end-to-end** — see note below.                  |
|**VendorCapacityManagementAgent**|LLM call over active-job-count vs. threshold to decide throttling. **Also currently broken end-to-end** — see note below.             |

Important, not in the README: VendorQualificationAgent and VendorCapacityManagementAgent are labeled “PLACEHOLDER” in README.md, but their real functions (score_vendor_qualification, manage_vendor_capacity) are fully implemented in handler.py and registered as FastAPI routes with proper operation_ids. However, VendorManagerAgents/MCP/main.py‘s include_operations list for both sub-apps still only exposes the old read-only tool (get_vendor_master, get_vendor_active_jobs) — so the LLM agent’s own prompt tells it to call a tool that was never bound to it. This is the exact same bug pattern I found in 6 of the SIU agents. On top of that, both handlers write to columns that don’t exist in vendor_master_input as declared in init_db.py (qualification_score, qualified_at, capacity_status, capacity_checked_at, capacity_threshold, throttle_threshold, license_valid, insurance_verified, certifications_current, background_check_passed are all referenced but never created) — so even called directly via HTTP, these two would currently fail against a fresh database.

2. Database read/write mapping

|Agent                            |Reads                                                             |Writes (Insert/Update)                                                                                                       |
|---------------------------------|------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------|
|**VendorOnboardingAgent**        |`vendor_applications`                                             |`vendor_applications` (insert/update status), `vendors` (insert, on approval), `vendor_master_input` (insert, on approval)   |
|**VendorMatchingAgent**          |`claims`, `vendors`, `vendor_master_input`                        |`vendor_assignment` (upsert)                                                                                                 |
|**VendorQualificationAgent**     |`vendor_master_input`                                             |`vendor_master_input` (update `assignment_eligible` + 2 undeclared columns — see above)                                      |
|**VendorCapacityManagementAgent**|`vendor_master_input`, `vendor_jobs_input`                        |`vendor_master_input` (update `assignment_eligible` + 2 undeclared columns)                                                  |
|**VendorCostBenchmarkAgent**     |`vendor_benchmarks`, `vendor_cost_input`                          |`vendor_cost_input` (insert), `cost_variance_output` (upsert)                                                                |
|**DispatchAgent**                |`work_orders`                                                     |`work_orders` (insert/update), `dispatch_logs` (insert per transition)                                                       |
|**VendorPerformanceAgent**       |`vendor_jobs_input`, `vendor_rating_input`, `cost_variance_output`|`vendor_rating_input` (insert), `vendor_performance_score_output` (delete+insert), `vendor_master_input` (update `vis_score`)|
|**SLAComplianceAgent**           |`vendor_jobs_input`, `vendors`                                    |`sla_tracker_output` (upsert)                                                                                                |
|**EscalationAgent**              |`vendor_jobs_input`                                               |`escalation_log_output` (insert per overdue job), `job_status_update_output` (upsert)                                        |
|**ETAPredictionAgent**           |`vendor_benchmarks`, `vendors`, `claims`                          |`eta_predictions` (insert)                                                                                                   |

Want me to package this as a doc, or is the chat answer sufficient for now?
