Good. I can see the entire flow now. Let me explain it exactly as your code executes.

Communication Agent Architecture

You have 3 layers:

User
  ↓
Communication Agent (8809)
  ↓
MCP Server (8800)
  ↓
Communication MCP Handler
  ↓
PostgreSQL Tables

⸻

Step 1: User sends message

Example:

For claim CLM-2026-1001:
I am really frustrated.
It has been 3 days and I have not heard anything.
What is going on with my claim?

Request goes to:

@app.post("/chat")
async def chat_stream(request: Request)

inside:

CommunicationAgent/server.py

⸻

Step 2: Agent loads tools

From:

tools = await get_tools()

which loads:

communication_mcp
feedback_mcp

Tools available:

get_communication_history
draft_status_notification
write_customer_feedback
get_customer_feedback
update_sentiment_tracker
get_sentiment_tracker

You can see that in logs:

Tools loaded from MCP:
[
 get_communication_history,
 draft_status_notification,
 write_customer_feedback,
 get_customer_feedback,
 update_sentiment_tracker,
 get_sentiment_tracker
]

⸻

Step 3: LLM receives prompt

Prompt:

_FALLBACK_PROMPT

Current prompt says:

1. Extract claim_number
2. Call draft_status_notification
3. Call get_communication_history
4. Call log_communication
5. If sentiment unavailable call update_sentiment_tracker
...

⸻

Important Issue

You DO NOT HAVE:

log_communication

tool.

But prompt says:

Call log_communication

So GPT may try to call a non-existent tool.

This should be removed.

⸻

Step 4: draft_status_notification()

First tool called.

Router:

POST
/api/communication/draft/{claim_number}

calls:

handler.draft_status_notification()

⸻

What data does it read?

Table 1

claims

Query:

SELECT *
FROM claims
WHERE claim_number=%s

Reads:

claim_number
status
claimant_name
policy_holder_name
loss_type

Example:

CLM-2026-1001
Open
John Smith
Water Damage

Stored into:

claim

⸻

Table 2

communication_history

Query:

SELECT *
FROM communication_history
WHERE claim_number=%s
ORDER BY communication_date DESC
LIMIT 5

Reads latest communications.

Example:

Email sent
SMS sent
Customer called
Adjuster update

Stored into:

history

⸻

Table 3

claim_sentiment_tracker

Query:

SELECT *
FROM claim_sentiment_tracker
WHERE claim_number=%s
ORDER BY updated_at DESC
LIMIT 1

Reads:

current_sentiment

Example:

Frustrated
Angry
Neutral
Satisfied

Stored into:

sentiment_data

⸻

Step 5: Prompt sent to Azure OpenAI

Agent builds:

prompt = (
    Claimant
    Claim Number
    Current Status
    Loss Type
    Sentiment
    Recent Communication Summaries
)

Example:

Claimant: John Smith
Claim Number:
CLM-2026-1001
Current Status:
Coverage Verification
Loss Type:
Water Damage
Policyholder Sentiment:
Frustrated
Recent Communications:
[
 "Adjuster assigned",
 "Inspection scheduled"
]

⸻

Step 6: GPT generates notification

GPT returns:

{
  "subject":"Update on Your Claim",
  "message_body":"We understand your frustration...",
  "channel":"Email",
  "tone":"Empathetic",
  "next_action":"Adjuster will contact you"
}

Stored in:

draft

⸻

Step 7: Communication History gets updated

New row inserted.

Table:

communication_history

Insert:

INSERT INTO communication_history
(
 claim_number,
 communication_date,
 channel,
 subject,
 message_body,
 direction,
 status,
 drafted_by
)

Values:

claim_number
communication_date
Email
subject
message_body
Outbound
Draft
CommunicationAgent

⸻

So what gets populated?

Communication Agent WRITES ONLY:

communication_history

New record:

claim_number
communication_date
channel
subject
message_body
direction
status
drafted_by

⸻

What does get_communication_history do?

Router:

GET
/api/communication/history/{claim_number}

calls:

handler.get_communication_history()

Query:

SELECT *
FROM communication_history
WHERE claim_number=%s
ORDER BY communication_date DESC

Returns:

[
 {
   communication_id:1,
   subject:"Update",
   message_body:"..."
 }
]

NO INSERT.

NO UPDATE.

Read only.

⸻

What is Feedback MCP doing?

Loaded here:

feedback_mcp

Tools:

write_customer_feedback
get_customer_feedback
update_sentiment_tracker
get_sentiment_tracker

Those are NOT in this handler.

They belong to:

Feedback Agent

and probably update:

customer_feedback
claim_sentiment_tracker

tables.

⸻

Actual Runtime Sequence

User says:

I am frustrated about claim CLM-2026-1001

Agent flow:

Communication Agent
        ↓
Extract claim number
        ↓
draft_status_notification
        ↓
Read claims
        ↓
Read communication_history
        ↓
Read claim_sentiment_tracker
        ↓
Call GPT
        ↓
Generate notification
        ↓
Insert draft into communication_history
        ↓
get_communication_history
        ↓
Return communication history
        ↓
Show drafted response

⸻

Current Problems in Your Code

Problem 1

Prompt says:

Call log_communication

No such tool exists.

Remove it.

⸻

Problem 2

Prompt says:

Call draft_status_notification

twice.

Step 2 and Step 7.

This causes duplicate drafts.

Remove second call.

⸻

Problem 3

Prompt says:

If sentiment unavailable
call update_sentiment_tracker

But it never explains how to pass:

claim_number
sentiment
feedback text

GPT may call tool incorrectly.

⸻

Tables Used By Communication Agent

Reads

claims

Fields used:

claim_number
status
claimant_name
policy_holder_name
loss_type

⸻

Reads

claim_sentiment_tracker

Fields used:

current_sentiment
updated_at

⸻

Reads

communication_history

Fields used:

message_text
message_body
summary
communication_date

⸻

Writes

communication_history

Fields populated:

claim_number
communication_date
channel
subject
message_body
direction
status
drafted_by

That’s the complete end-to-end flow of your Communication Agent based on the code you’ve shared.
